C-6 실측 채증 (서버 상태 ② — useMutation·낙관적 업데이트)
=========================================================

측정 환경
  @tanstack/react-query 5.101.4 (registry latest 와 동일)
  @tanstack/react-query-devtools 5.101.4 (C-5 준비 커밋에서 이미 devDependency)
  axios 1.19.0 · react-router 8.3.0 · React 19.2.8 · TS 6.0.3 · Vitest 4.1.10
  착수 시점 테스트 기준선: 104 files / 792 passed (단독 실행)


[0] 시의성 — 뮤테이션 콜백 시그니처가 5.89.0 에서 바뀌었다
--------------------------------------------------------
근거: TanStack/query issue #9660 · PR #9615
      공식 문서 optimistic-updates 예제(2026-08-14 확인)

설치본 .d.ts 가 적고 있는 것:
  mutationFn: (variables, context) => Promise<TData>
  onMutate:   (variables, context) => TOnMutateResult
  onSuccess:  (data, variables, onMutateResult, context)
  onError:    (error, variables, onMutateResult, context)
  onSettled:  (data, error, variables, onMutateResult, context)

  MutationFunctionContext = { client: QueryClient; meta; mutationKey? }
  제네릭 이름도 TContext -> TOnMutateResult 로 개명

시중 자료(그리고 5.89.0 이전 문서)는 전부 3인자다:
  onError: (err, variables, context) => ...   // 여기서 context = onMutate 반환값

런타임 실측 (scratch/c6-mutation-signature-probe.test.tsx · 5/5 통과)
  - mutationFn 이 받는 인자 개수 = 2. 두 번째의 키는 정확히
    ['client', 'meta', 'mutationKey'] 이고 client 는 그 판의 QueryClient 와 동일 객체
  - onMutate 의 두 번째 인자로 온 context.client.setQueryData 가 실제로 캐시를 바꾼다
    (useQueryClient() 를 안 불러도 된다)
  - onError 가 받는 인자 개수 = 4 · onSettled = 5
    onError[2] = onMutate 가 돌려준 값 · onError[3].client = 그 판의 QueryClient
  - *** 옛 3인자 형태는 그대로 동작한다 *** — 위치가 같아서 값도 같다.
    onMutate 스냅샷 -> onError 롤백이 정상 복원됨('처음' 으로 되돌아옴)

타입 실측 (scratch/c6-expected-errors.tsx)
  npx tsc --noEmit --ignoreConfig --jsx react-jsx --strict --module esnext \
          --moduleResolution bundler --target es2025 --skipLibCheck \
          scratch/c6-expected-errors.tsx

  ① 옛 3인자 형태 (세 번째를 context 라 명명, .previous 만 씀)  -> 0건 (통과)
  ② 옛 이름 + 새 문서 사용법 섞기 (세 번째에 .client)          -> 2건
       (32,7)  TS18048: 'context' is possibly 'undefined'.
       (32,15) TS2339: Property 'client' does not exist on type 'Snapshot'.
  ③ 새 문서 형태 (네 번째를 context 로 받아 .client)            -> 0건 (통과)

  => 옛 코드는 안 터진다. 이름만 거짓말이 된다.
     그리고 옛 이름 + 새 사용법을 섞는 순간 타입 검사가 막는다.
     *** C-2 의 useLoaderData(any 라 안 막는다) 와 정반대 자리다. ***


[1] 착수 전제 — 앱에 출입증을 받을 길이 없다
--------------------------------------------
  grep 결과: login() 은 src/api/c5-auth-interceptor.test.ts 에서만 불린다.
  화면(*.tsx) 어디에도 호출부가 0건.
  스텁 서버는 POST /posts/:id/like 에 토큰을 요구한다(없으면 401 '로그인이 필요합니다').
  -> 사용자 승인(2026-08-14)으로 Layout 머리말에 출입증 버튼을 둔다.
     진짜 로그인 화면은 G-1 의 몫이라고 교안이 명시한다.

  스텁 서버 규약(C-5 에서 확인한 것 재기록)
    지연 400ms · 좋아요는 5번에 한 번 500 · 액세스 토큰은 3회 사용 후 401
    남의 댓글 삭제는 403 · 댓글은 post 1 에 남의 것 둘(minji·seungwoo),
    post 2 에 내 것 하나(jaehoon = ME)


[2] Step 별 실측
----------------

Step 1 — 좋아요를 서버에 보낸다 (useMutation)
............................................

  코드: api/posts.ts likePost() · queries/posts.ts useLikeMutation()
        components/SignInButton.tsx (신설) · routes/Layout.tsx · components/FeedSection.tsx

  *** 예측이 틀렸다 — StrictMode 에서 useQuery 는 두 번 안 나간다 ***
    같은 StrictMode 판에서
      effect+fetch 박제(HomePageByEffect) : GET /api/posts 2번
      useQuery(HomePage)                  : GET /api/posts 1번
    도구가 같은 키로 겹친 요청을 하나로 합친다.
    처음에 "읽기도 두 번" 으로 단언을 썼다가 1 이 나와서 실측대로 고쳤다.
    -> 교안에서 이 대비가 오히려 더 좋아졌다: 읽기는 합쳐주고 쓰기는 안 합쳐준다.

  쓰기는 누른 만큼 나간다
    하트 두 번 클릭 -> POST 2번 -> 서버 likeCount 1240 -> 1241 -> 1240, liked false
    (뮤테이션에는 키가 없다. 합칠 근거가 없다.)

  출입증 없이 누르면
    POST 는 나가고 401 이 온다. 갱신할 리프레시 토큰도 없어 실패로 끝난다.
    서버 상태는 그대로(liked false / 1240). 화면만 혼자 바뀌어 있다.

  서버가 기억한다
    로그인 후 하트 -> fakeDb 의 1번 글이 liked true / 1241 로 바뀐다.
    캐시를 안 거치고 fetchFeed() 로 다시 물어봐도 1241 이 온다.

  blast radius — 12 파일 44 자리 (단언 무변경)
    원인은 하나: FeedSection 이 useLikeMutation 을 부르면서 QueryClient 문맥을 요구한다.
    withQuery(withRouter(...)) 로 감싸기만 했다.
    a4-typing 3 · a5-toast-reset 2 · a6-icon-button 1 · a6-list 2 · b1-render 1 ·
    b2-form 2 · b2-lifting 4 · b4-toast 3 · e4-dark-mode 2 · e7-visual-polish 6 ·
    b3-use-like-toggle 1 · c5-effect-fetch 10 · c5-stale-server-state 8 · c1-link 1
    (+ a5-toast-reset 의 liveMsAfterSecondToast 헬퍼 안쪽 1자리는 정규식이 못 잡아 손으로)
    ⚠️ C-4 함정 ① 회피 — StrictMode 판은 <StrictMode>{withQuery(...)}</StrictMode> 로
       안쪽에 넣었다(바깥에 씌우면 이중 실행 관찰이 깨진다).

  *** 모듈 간 불변식이 또 걸렸다 ***
    b3-use-like-toggle 의 "첫 화면 HTML 이 옮기기 전과 글자 하나 안 다르다" 가
    머리말에 붙은 '로그인' 때문에 깨졌다. 이 판은 이미 E-5(밝기)·C-1(메뉴)이
    같은 이유로 withoutThemeToggle·withoutSiteNav 를 두고 있었다.
    같은 idiom 으로 withoutSignIn 을 추가하고 not.toContain('로그인') 가드를 붙였다.
    단언 본체는 그대로.

  *** C-5 에서 넘어온 결함 1건 (내가 만든 게 아니다) ***
    src/routes/c5-answer.test.tsx:8 의 import { http, HttpResponse } 가 미사용.
    eslint 2건(@typescript-eslint/no-unused-vars). 워킹트리 diff 0 = 커밋된 상태 그대로였다.
    => C-5 canon 의 "eslint 0" 기록이 실제와 안 맞았다. 이번에 제거.

  게이트: 106 files / 805 passed · tsc 0 · eslint 0 (전부 단독 실행)


Step 2 — 쓰고 나면 읽어둔 것이 낡는다 (invalidateQueries + DevTools)
....................................................................

  *** Step 순서를 원안에서 맞바꿨다 (2 = 무효화, 3 = 사본 걷어내기) ***
    원안은 2 = 사본 걷어내기 -> 3 = 무효화였다. 그런데 사본을 먼저 걷어내려고
    보니 b2-lifting("카드에서 누른 좋아요가 헤더 숫자까지 올라온다" 등 4건)·
    b4-toast(알림 2건 + 탭 제목 1건)·a5-toast-reset·b3-use-like-toggle 이
    FeedSection 의 *지역 상태 동작 자체* 를 단언하고 있었다.
    사본을 걷어내면 그 단언들을 고쳐야 하는데, 그건 금지된 쪽이다.
    -> 순서를 바꾸면 버그를 *증명한 뒤* 고치는 흐름이 되고, 학생 동기도 훨씬 낫다.
       ("무효화를 했는데 왜 화면이 그대로죠?")

  코드: queries/posts.ts useLikeMutation 에 onSuccess -> invalidateQueries(['posts'])
        main.tsx 에 ReactQueryDevtools (import.meta.env.DEV 가드)

  ⚠️ 새 시그니처를 실제로 채택했다
    onSuccess: (_result, _postId, _onMutateResult, context) =>
      context.client.invalidateQueries(...)
    useQueryClient() 를 안 부른다. 위 [0] 의 실측 근거대로.

  실측
    좋아요 성공 -> GET /api/posts 가 1 -> 2 번 (무효화가 다시 물어본다)
    staleTime 30초를 줘도 무효화는 나간다 (1 -> 2) — C-5 Step 8 회수
    좋아요 실패(500) -> GET 은 1번 그대로 (onSuccess 라서 안 돈다. Step 5 가 onSettled 로 고친다)

  *** 이 Step 의 교보재 — 무효화를 했는데 화면이 안 바뀐다 ***
    시나리오: 화면이 1240 을 받는다 -> 보는 동안 다른 사람들이 눌러 서버가 1300 이 된다
              -> 우리가 하트를 누른다 -> 서버는 1301 을 돌려준다
    결과:     캐시(getQueryData(['posts'])[0].likeCount) = 1301
              화면                                      = 1241   <- 자기가 센 숫자
    화면에 '좋아요 1301개' 는 아예 없다.
    원인: FeedSection 의 useReducer 초기값은 첫 렌더에 한 번만 읽힌다(A-5·C-5 Step 1 회수).
          나중에 도착한 배열이 안 들어간다. => Step 3 의 존재 이유.

  ★ DevTools 는 배포본에 안 들어간다 (실측)
    dist/assets/index-*.js 에서 'ReactQueryDevtools'·'query-devtools' grep 0건.
    import.meta.env.DEV 가드가 실제로 잘라낸다.

  번들: JS 598,150 -> 601,448 B (+3,298) · CSS 42,480 -> 42,488 B (+8)
    ⚠️ CSS 가 C-2 이후 처음으로 움직였다. C-3·C-4·C-5 는 전부 불변이었다
       (데이터 계층은 CSS 를 안 만든다). C-6 은 머리말에 버튼을 하나 더해서다.
       다만 +8 B 의 정확한 출처(어느 유틸리티)는 아직 안 짚었다 — 교안이 이 숫자를
       인용하지는 않으므로 보류.

  게이트: 107 files / 809 passed · tsc 0 · eslint 0 · build 성공


Step 3 — 진실이 둘이면 하나는 거짓말 (사본을 걷어낸다)
.......................................................

  결정: 박제 후 전환 (사용자 승인 2026-08-14)
    scratch/c6-feed-section-before.tsx 에 FeedSectionBeforeServer 를 두고,
    지역 동작을 단언하던 판들이 그것을 보게 했다. *단언은 한 줄도 안 고쳤다.*

  코드: components/FeedSection.tsx — useFeed/useReducer 제거.
        posts 는 props(창고) 그대로 · likedCount 도 props 에서 셈 ·
        좋아요는 likeMutation.mutate 만 · 알림만 useState 로 남김
        lib/feed-state.ts — likeToastMessage()/allSeenToastMessage() 순수 추출
        (feedReducer 출력 불변. a5-feed-state·a5-answer 그대로 통과)

  ★ Step 2 에서 갈렸던 숫자가 맞는다
    같은 시나리오(1240 -> 다른 사람들이 눌러 1300 -> 우리가 누름 -> 서버 1301)
      Step 2: 캐시 1301 / 화면 1241
      Step 3: 캐시 1301 / 화면 1301   ('좋아요 1241개' 는 화면에 없음)

  ★ 서버가 거절하면 화면도 안 바뀐다 (likeFailEvery=1 -> 500)
    숫자가 1240 그대로. 혼자 앞서가지 않는다.
    => 이것이 Step 4(낙관적 업데이트)의 반대 극단이다. 0.4초 동안 아무 반응이 없다.

  blast radius — 9건 / 8파일 (단언 무변경)
    박제로 옮긴 것:
      b2-lifting   describe('HomePage — 상태를 끌어올린 뒤') 4건
      b4-toast     describe('HomePage 의 탭 제목') 1건
      a4-typing    describe('PostCard — 이미지 더블클릭으로 좋아요') 2건
      e7-visual-polish '누르면 같은 이름의 버튼이 눌린 상태로' 1건
      b3-use-like-toggle '좋아요를 누르면 머리말 숫자가' 1건
      c5-effect-fetch  describe('가져온 뒤에 넘기는 자리 — 초기값은 한 번만 읽힌다') 2건
      c6-invalidate    Step 2 의 버그 관찰 1건 -> HomePageBeforeSingleTruth
    살아 있는 화면에 그대로 둔 것:
      b4-toast 의 알림 2건 (알림은 여전히 지역이라 진짜로 통과한다)
      a4-typing·e7 의 마크업 판들 (첫 렌더 결과가 같다)

  *** C-5 의 박제가 미완성이었다 ***
    scratch/c5-effect-fetch-before.tsx 가 *살아 있는* FeedSection 을 그리고 있었다.
    그래서 C-6 이 FeedSection 을 바꾸자 C-5 Step 1 의 교보재
    ("나중에 도착한 배열을 다시 넘겨도 화면은 안 바뀐다")가 깨졌다.
    박제는 자기가 재는 성질을 가진 컴포넌트까지 함께 얼려야 한다.
    -> c5-effect-fetch-before 가 FeedSectionBeforeServer 를 그리게 고쳤다.

  ⚠️ 헛통과 발견
    b2-lifting 의 '한 번 더 누르면 헤더도 함께 되돌아온다'·'한 카드를 눌러도
    다른 카드는 그대로다' 는 사본을 걷어낸 뒤에도 *통과했다* — 아무 일도 안 일어나는데
    기대값이 우연히 같아서다. describe 를 통째로 박제로 옮겨 실제로 재게 했다.
    (C-1 canon 의 "없는 것을 지우고 없다고 단언" 과 같은 계열)

  번들: JS 601,448 -> 600,622 B (-826 · 살아 있는 경로에서 useFeed 가 빠졌다)
        CSS 42,488 B 불변

  게이트: 108 files / 814 passed · tsc 0 · eslint 0 · build 성공


Step 4 — 기다리지 않고 먼저 바꾼다 (onMutate + setQueryData + cancelQueries)
............................................................................

  코드: queries/posts.ts useLikeMutation 에 onMutate 추가.
        cancelQueries(feedKey()) -> setQueryData(feedKey(), toggleLike(...))
        낙관적 갱신에 B-2 의 순수 함수 toggleLike 를 그대로 재사용한다.

  실측
    좋아요 400ms 지연: 누른 즉시 화면 1240 -> 1241 (서버는 아직 1240)
    하트도 그 자리에서 aria-pressed=true
    답이 오면 서버 값으로 갈린다 (다른 사람들이 눌러 1300 이었으면 1241 -> 1301)
    ⚠️ 지연이 없으면 낙관적 값이 눈에 안 잡힌다 — 답이 먼저 도착해 덮어쓴다.
       테스트에서 중간 상태를 보려면 지연을 줘야 한다.

  *** 처음 쓴 cancelQueries 테스트는 가짜였다 ***
    "늦게 온 옛 답이 덮어쓰지 않는다" 를 최종 값으로 단언했는데,
    cancelQueries 를 통째로 제거해도 4건 전부 통과했다.
    끝나고 보면 어느 쪽이든 onSuccess 무효화가 서버 값으로 맞춰주기 때문이다.
    => 갈리는 것은 *마지막 값* 이 아니라 *가는 길에 되돌아가느냐* 였다.

  캐시 값의 흐름을 통째로 찍어서 실측 (scratch/c6-cancel-probe.test.tsx)
    조건: 피드 200ms · 좋아요 700ms · refetch 를 띄워놓고 그 사이에 클릭
      cancelQueries 있음: [1241]
      cancelQueries 없음: [1241, 1240]        <- 옛 답이 덮어쓴 자국
    상시 테스트를 흐름 단언(toEqual([1241]))으로 고쳐 쓰니
    가드 제거 시 [1241, 1240, 1241] 로 실패한다. 화면으로는
    "빨개졌다가 잠깐 꺼졌다가 다시 켜진다".

  ⚠️ Step 3 의 관찰 하나가 뒤집혔다
    "서버가 거절하면 화면도 안 바뀐다" 는 낙관적 업데이트를 넣는 순간 거짓이 된다.
    이제 거절당해도 화면은 바뀐 채로 남는다 -> 그것이 Step 5 의 존재 이유다.
    Step 3 테스트 파일에서 그 판을 뺐다(그 시점 코드는 Step 3 커밋에 남아 있다).

  게이트: 110 files / 819 passed · tsc 0 · eslint 0


Step 5 — 거절당하면 되돌린다 (이전 값 보관 + onError + onSettled)
..................................................................

  코드: onMutate 가 getQueryData 로 previous 를 챙겨 { previous } 를 돌려준다
        onError 가 그 세 번째 인자로 setQueryData 복원
        onSuccess -> onSettled 로 교체

  *** 또 가짜 성과를 쓸 뻔했다 — onError 를 통째로 빼도 5건 전부 통과했다 ***
    onSettled 무효화가 어차피 서버 값으로 맞춰주기 때문이다.
    => 갈리는 것은 "되돌아오느냐" 가 아니라 "언제 되돌아오느냐" 였다.

  시간축 실측 (scratch/c6-cancel-probe.test.tsx · 좋아요 200ms 거절 · 피드 800ms)
      롤백 있음: 1241@0ms -> 1240@200ms     (거절당한 그 순간)
      롤백 없음: 1241@0ms -> 1240@1000ms    (피드를 다시 받아온 뒤)
    *** 800ms 차이. 그동안 사용자는 좋아요가 눌린 줄 안다. ***
    상시 테스트를 "거절 직후 500ms 안에 1240" 으로 고치니
    onError 제거 시 실패한다(expected 1241 to be 1240).

  onSettled 인 이유 — 실측 2건
    ① 실패해도 GET /api/posts 가 1 -> 2 (Step 2 의 onSuccess 판은 1 이었다)
    ② 되돌린 값이 틀렸어도 서버가 바로잡는다
       우리 요청은 거절당했는데 그사이 다른 사람들이 눌러 진짜 값이 1300 인 판:
       롤백만 하면 1240 인데, 무효화가 뒤따라서 화면이 1300 으로 맞는다.

  ⚠️ Step 2 의 관찰 하나가 뒤집혔다
    "실패하면 안 물어본다 — onSuccess 라서" 는 onSettled 로 바꾸는 순간 거짓이 된다.
    Step 2 테스트 파일에서 그 판을 뺐다(그 시점 코드는 Step 2 커밋에 남아 있다).

  게이트: 111 files / 826 passed · tsc 0 · eslint 0


Step 6 — loader 와 useQuery 의 화해 (ensureQueryData)
.......................................................

  코드: queries/posts.ts 에 postQuery(id) = queryOptions({ queryKey:['posts',id], queryFn })
        routes/postLoader.ts — 흉내 함수 fetchPost 를 버리고
          queryClient.ensureQueryData(postQuery(id)) 로 창고를 채우고 그 값을 돌려준다
          ApiError(404) 를 new Response(error.message, {status:404}) 로 바꿔 던진다
        routes/PostDetailPage.tsx — useLoaderData 를 initialData 로 주고 useQuery 로 읽는다

  *** 초안이 C-2 의 학습 결론 두 개를 거짓으로 만들었다 ***
    처음에는 loader 가 null 을 돌려주고 화면이 useQuery 만 쓰게 했다. 그러자
      ① c2-loader "useLoaderData 에 무엇을 담았는지 알려준다 — 안 알려주면 any 다"
         -> PostDetailPage 에서 useLoaderData 가 통째로 사라져 거짓
      ② c2-not-found "상세는 이제 못 찾은 경우를 신경 쓰지 않는다"(소스에 undefined 0건)
         -> useQuery 의 data 가 Post|undefined 라 if (post === undefined) 가 생겨 거짓
    => loader 가 값을 돌려주고 그것을 initialData 로 건네는 모양으로 고쳤다.
       initialData 가 있으면 data 타입이 non-undefined 라 갈래가 안 생긴다.
       C-4 의 "읽는 쪽을 PostBody 로 내렸다" 와 같은 성격의 판단.

  실측
    /p/1 첫 화면부터 게시물이 들어 있다 ("불러오는 중" 0건)
    GET /api/posts/1 은 한 번만 나간다 (loader 가 채운 것을 화면이 그대로 읽는다)
    ★ 이미 받아둔 게시물이면 요청이 아예 안 나간다 (ensureQueryData 가 캐시를 존중)
    없는 번호(/p/999) -> 서버 사유가 404 화면에 "404 게시물을 찾을 수 없습니다"
    번호가 아닌 주소(/p/abc) -> loader 가 먼저 막아 요청 0건

  blast radius — 11건 / 6파일 (C-2·C-3 판)
    (a) 서버를 붙였다: c2-error-boundary · c2-navigate · c2-post-detail · c3-provider-scope
        (/p/:postId 가 이제 진짜 서버에 붙는다. 보던 것은 그대로)
    (b) 타이밍: c2-loader '가는 중에는 앞 화면이 그대로 남아 있다'
        *** C-2 의 흉내 함수는 400ms 를 기다렸는데 진짜 서버 하네스는 0ms 다. ***
        "가는 중" 이라는 순간 자체가 사라져 실패했다 -> postFromDb(300) 으로 지연을 준다.
        단언 무변경.
    (c) 단언 재조준 1건: c2-not-found 의
        toMatch(/new Response\('게시물을 찾을 수 없습니다'/) -> /new Response\(error\.message/
        + not.toMatch(/statusText/) 를 추가해 더 강하게.
        한글 문구의 출처가 클라이언트 리터럴에서 서버로 옮겨갔기 때문이다.
        (C-2 가 c1-layout·c1-link 단언을 재조준한 선례와 같은 성격)

  게이트: 112 files / 833 passed · tsc 0 · eslint 0
