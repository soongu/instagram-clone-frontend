F-2 (첫 테스트를 쓴다 — describe/it/expect 와 vi.*) 채증
브랜치: module-F2-first-tests (module-F1-vitest-basics 3825d21 에서 분기)
측정일: 2026-08-19

────────────────────────────────────────────────────────────────────────
1. 착수 전제 확인 — 세 갈래가 그대로였고, 네 번째가 나왔다
────────────────────────────────────────────────────────────────────────

F-1 마무리가 예고한 세 갈래를 전부 재확인. 셋 다 살아 있었다.
그리고 사람 조건(103행) 표를 다시 읽으니 ①과 거의 같은 모양의 행이 하나 더 있었다.

  ① fetchPost         data/feed.ts:157   44.44 / 100  /   0 / 57.14
  ② never             lib/comments.ts    66.66 / 66.66/ 100 / 66.66  (38-39)
  ③ toggleLikeInPlace lib/likes.ts        100  /  75  / 100 /  100   (8-10)
  ④ fetchConversations api/dm.ts            50  / 100  /  50 /  50   (13-15)

①과 ④는 표에서 둘 다 "아무도 안 부른다" 인데 답이 정반대다.
  ① = C-6 이 진짜 서버로 옮기며 남긴 죽은 코드. 살아 있는 것은 api/posts.ts 의 fetchPostById
  ④ = 쓰이는 fetchMessages 옆에 나란히 선 살아 있는 API 면
표는 이 둘을 구분해주지 못한다. 가르는 것은 이력과 의도뿐이다.

  ⇒ 이 회차의 척추: 커버리지는 「안 덮였다」까지만 말하고
     「그래서 무엇을 하라」는 안 말한다.

────────────────────────────────────────────────────────────────────────
2. ① fetchPost — 지운다
────────────────────────────────────────────────────────────────────────

src/ 전체에서 `fetchPost` 매치 = 1건 (자기 정의뿐).
전용 도우미 delay() 와 NETWORK_DELAY_MS 도 이 함수 말고는 아무도 안 쓴다
  → 12줄이 함께 사라진다 (feed.ts 160줄 → 148줄).

삭제 후: tsc 0 · eslint 0 · npm test 135 files / 954 passed (변화 0)

⚠️ 셸 함정 재현 (F-1 교훈) — `grep -rn "fetchPost" src/ --include=*.ts` 에서
zsh 가 --include=*.ts 를 glob 으로 확장하려다 매치가 없어 명령 전체를 중단시켰다.
"호출자 0건" 이라는 빈 출력이 나왔는데 그건 grep 이 안 돈 결과였다.
따옴표로 묶거나 --include 를 빼고 다시 확인할 것.

────────────────────────────────────────────────────────────────────────
3. ② never — 못 덮는다, 그리고 덮으면 안 된다
────────────────────────────────────────────────────────────────────────

lib/comments.ts:36-40
    default: {
      const missed: never = action;
      return missed;
    }

[정직하게 부르면] tsc 가 막는다
  commentReducer(initialCommentState, { type: 'unknown' })
  → TS2322: Type '"unknown"' is not assignable to type '"add" | "remove"'.
  (탐침: @ts-expect-error 를 넣으면 exit 0, 빼면 exit 2 — 양방향 확인)

⚠️ 이 확인을 처음에 scratch/ 에서 했다가 무효였다.
   tsconfig.json 의 include 는 ["src", "vite.config.ts", "vitest.config.ts"] 라
   scratch 를 아예 안 본다. exit 0 이 "통과" 가 아니라 "안 봄" 이었다.
   → src/ 로 옮겨 재측정. (C-3 canon 의 같은 함정 재현)

[거짓말로 캐스팅하면] 런타임에 닿는다
  const lie = { type: 'unknown' } as unknown as CommentAction;
  commentReducer(initialCommentState, lie)
  → {"type":"unknown"}   ← 상태가 아니라 액션 그 자체가 돌아온다

[그 판을 쓰면] lib/comments.ts 66.66/66.66/100/66.66 → 100/100/100/100
  숫자는 오르고 판이 지키는 것은 없다.
  보관: scratch/f2-never-experiment.ts.txt (저장소에 두지 않는다)

────────────────────────────────────────────────────────────────────────
4. ③ toggleLikeInPlace — 진짜 구멍
────────────────────────────────────────────────────────────────────────

기존 판(components/b2-lifting.test.tsx:122)은 있는 번호로만 부른다.
없는 번호일 때 어떻게 되는지 아무도 안 물어봤다.

[실측] toggleLikeInPlace(posts, 999)
  next === posts        → true   (받은 배열을 그대로 돌려준다)
  next[0] === posts[0]  → true
  likeCount / liked     → 1240 / false  (안 바뀐다)

⚠️ 구멍을 메우고도 100% 가 아니었다 — 75 → 87.5, 남은 것은 line 10.
   if 의 갈래를 덮었더니 그 안의 삼항이 남았다 (감소 쪽).
   "이미 눌린 것을 다시 누르면 내려간다" 를 더해서 100/100/100/100.
   ⇒ 한 구멍이 두 겹이었다. 커버리지가 한 번에 다 안 알려준다.

⚠️ % Lines 100 인데 % Branch 75 였다 — 모든 줄이 실행됐는데 갈래는 안 덮인 자리.
   줄 커버리지와 갈래 커버리지가 갈리는 것을 그 자리에서 보여준다.

────────────────────────────────────────────────────────────────────────
5. 없는 번호에서 두 함수의 우열이 뒤집힌다 (Step 5)
────────────────────────────────────────────────────────────────────────

                          next === posts   next[0] === posts[0]
  toggleLikeInPlace(999)      true                true      (안 만든다)
  toggleLike(999)             false               true      (새 배열)

B-2 는 map 버전이 옳다고 가르쳤고 그건 번호가 있을 때 참이다.
그런데 아무것도 안 바뀌는 가장자리에서는 map 버전이 헛되이 새 배열을 만들어
React 가 "바뀌었다" 고 보게 만든다. 그 가장자리를 아무도 안 물어봤다.

⚠️ B-2 를 부정하지 않게 서술할 것 — 번호가 있으면 여전히 map 이 옳다.

[가드 확인] not.toBe 를 not.toEqual 로 바꾸면 FAIL 1건 → 두 매처가 실제로 갈린다

────────────────────────────────────────────────────────────────────────
6. ④ fetchConversations — vi.mock 으로 갈아 끼운다 (Step 6)
────────────────────────────────────────────────────────────────────────

vi.mock('./client', () => ({ api: { get: vi.fn() } }))
→ 진짜 서버 없이 돈다. 봉투를 벗긴 배열이 그대로 나온다.

⚠️ 끌어올림(hoisting) 실측 — vi.mock 을 import 아래에 써도 통한다.
   탐침: import 뒤에 vi.mock 을 두고 실행 → 가짜가 쓰였다 ([{"id":9}]).
   Vitest 가 파일 맨 위로 끌어올리기 때문. 학생이 "위에 써야 한다" 로 외우면
   왜 통하는지를 모른 채 외우는 것이라 🙋 로 짚었다.

⚠️ 이 저장소의 기존 vi.fn 사용 59건이 전부 RTL(render + userEvent) 안이다.
   비-DOM 사용은 F-2 가 처음. F-3 를 선행하지 않으려면 기존 판을 인용하면 안 된다.
   (vi.mock 은 4건 — c7-auto-memo 2 · c7-stable-identity 1 · c7-answer 1,
    전부 importOriginal 을 쓰는 부분 교체라 우리 통째 교체와 형태가 다르다)

────────────────────────────────────────────────────────────────────────
7. mock.calls — 무엇을 물었는지 (Step 7)
────────────────────────────────────────────────────────────────────────

fetchMessages(7) → api.get 이 '/conversations/7/messages' 로 불렸다
fetchMessages(1), fetchMessages(2) → calls = [['/conversations/1/messages'],
                                              ['/conversations/2/messages']]

[가드 확인] dm.ts 의 방 번호를 1로 박아두면
  × 방 번호가 주소에 들어간다      FAIL
  × 방마다 다른 주소로 묻는다      FAIL
  ✓ 한 번만 묻는다                 통과   ← 횟수만 세는 판은 못 잡는다
  ✓ 봉투를 벗긴 배열이 나온다      통과   ← 돌려받은 값도 못 잡는다

⇒ 남의 방 쪽지를 가져오는 버그인데 결과 모양은 똑같다. 인자를 봐야만 잡힌다.

⚠️ beforeEach 에서 mockReset() 필요 — 가짜는 기록을 계속 쌓는다.
   안 지우면 "한 번만 묻는다" 가 앞 판 때문에 실패한다.

────────────────────────────────────────────────────────────────────────
8. watch 모드 (Step 8) — 🚨 내 조건과 학생 조건이 다르다
────────────────────────────────────────────────────────────────────────

vitest/dist/chunks/defaults.9aQKnqFk.js:48
    watch: !isCI && process.stdin.isTTY && !isAgent

  내 환경   : isAgent = true  · stdin.isTTY = undefined  → watch = 꺼짐
  학생 환경 : isAgent = false · TTY                      → watch = 켜짐

⇒ `npx vitest` (run 없이) 는 학생에게는 watch 로 들어가고 나에게는 안 들어간다.
   Step 8 의 모든 출력은 env -u AI_AGENT -u CLAUDECODE -u CLAUDE_CODE 로 재서 썼다.
   (F-1 의 커버리지 표 45행 vs 103행 사고와 같은 뿌리 — isAgent)

[사람 조건 실측]
  첫 실행                     465ms  ✓ 6 passed
  → likes.ts(소스)를 고침     75ms   × 2 failed   ← 테스트가 아니라 소스를 고쳤는데 돌았다
  → 되돌림                    71ms   ✓ 6 passed
  화면 아래: "PASS  Waiting for file changes... press h to show help, press q to quit"

  6배 빠른 이유 = 이미 켜둔 판을 다시 안 세운다(transform·setup·environment 재사용).

────────────────────────────────────────────────────────────────────────
9. 네 행의 전후 (사람 조건 103행 기준)
────────────────────────────────────────────────────────────────────────

  행                 전                          후                    무엇을 해서
  data/feed.ts       44.44/100  /  0 /57.14  →  100/100/100/100      지워서
  lib/comments.ts    66.66/66.66/100/66.66  →  그대로                안 덮어서
  lib/likes.ts        100 / 75  /100/ 100   →  100/100/100/100      판을 써서
  api/dm.ts            50 /100  / 50/  50   →  100/100/100/100      갈아 끼워서

  전체  89.75/82.83/94.32/94.97  →  90.06/82.97/95.77/95.35

⇒ feed.ts 가 100% 가 된 방법은 테스트가 아니라 삭제다.
   커버리지를 올리는 방법 중 하나는 코드를 지우는 것.

────────────────────────────────────────────────────────────────────────
10. 게이트
────────────────────────────────────────────────────────────────────────

  npm test        137 files / 964 passed   (F-1: 135 / 954 · +2 파일 +10 판)
  test:coverage   exit 0
  tsc --noEmit    0
  eslint          0
  web-next / api-stub  무변경

  커밋 (Step 시점):
    7566e2f  Step 2  fetchPost 삭제
    adb1e55  Step 4  첫 판
    7dfda90  Step 5  toBe / toEqual
    eafc2fe  Step 6  vi.mock
    09da5d5  Step 7  mock.calls
  Step 1·3·8 은 코드 변경 없음 (측정 · 실험보관 · watch)
