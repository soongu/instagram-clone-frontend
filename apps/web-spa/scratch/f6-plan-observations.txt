F-6 (Storybook) 계획 단계 채증
측정일: 2026-08-22
무대: apps/web-spa (Vite 8.1.5 + React 19.2.8 + Vitest 4.1.10)
브랜치: module-F6-storybook (module-F5-playwright 에서 분기)

================================================================
1절. 착수 기준선 (두 번 재서 대조)
================================================================
npm test 1회차: 152 files / 1061 passed (18:46:07, 20.83s)
npm test 2회차: 152 files / 1061 passed (18:46:54, 18.92s)
=> 핸드오프 기준선과 일치. 두 번 모두 동일.

포트: 3000 / 5173 / 6006 / 8090 / 9000 전부 비어 있음.
워킹트리: clean.

================================================================
2절. 시의성 — 「낡았나」와 「실재하나」 둘 다 (npm registry)
================================================================
storybook            latest = 10.5.10   (dist-tags: v9=9.1.20, v8=8.6.18, next=10.6.0-alpha.7)
@storybook/react-vite        10.5.10
@storybook/addon-vitest      10.5.10
@storybook/addon-a11y        10.5.10
@storybook/addon-docs        10.5.10
@chromatic-com/storybook     5.3.0
@vitest/browser              4.1.11 (설치는 4.1.10 — 5절 참조)
@vitest/browser-playwright   4.1.11 (설치는 4.1.10 — 5절 참조)
msw-storybook-addon          3.0.0 (2026-07-24 수정 · peer msw>=2 · storybook>=9)

=> 커리큘럼의 「Storybook 10」은 실재한다. H-9 의 MF 3.0 사고(없는 버전을 적었다)와
   갈리는 자리 — 이번엔 원안이 맞았다. 보정 불필요.

peer 확인:
  @storybook/react-vite@10.5.10 → vite "^5||^6||^7||^8"  → 우리 8.1.5 범위 안 ✓
                                   react "^16.8||^17||^18||^19" → 19.2.8 ✓
  @storybook/addon-vitest@10.5.10 → vitest "^3||^4" ✓
                                    @vitest/browser "^3||^4"
                                    @vitest/browser-playwright "^4.0.0"  ← F-5 가 안 깐 것

================================================================
3절. storybook init 이 에이전트를 감지한다 (--agent / --no-agent)
================================================================
`storybook init --help` 에 플래그가 있다:
  --agent      Force agent mode (non-interactive, logs AI setup instructions)
  --no-agent   Force disable agent mode even when an AI agent is detected

=> 도구가 실행 주체를 보고 동작을 바꾼다. F-1(커버리지 표 45↔103행)·F-2(watch 기본값)·
   F-3(console.log 소멸)·H-1~H-4 에 이은 계열. 이번엔 「출력」이 아니라 「설치 절차」다.
   그래서 모든 실행을 env -u AI_AGENT -u CLAUDECODE -u CLAUDE_CODE + --no-agent 로 했다.

================================================================
4절. 🚨 init 이 "완료" 라고 말하고 exit 0 인데 설치가 실패했다
================================================================
실행:
  env -u AI_AGENT -u CLAUDECODE -u CLAUDE_CODE npx storybook@10.5.10 init \
      --yes --no-dev --no-agent --disable-telemetry --features docs test a11y

출력 요약:
  ■ Installation of dependencies failed!
  ...
  ✅ @chromatic-com/storybook
  ❌ @storybook/addon-vitest      ← 커리큘럼 헤드라인이 이것 위에 서 있다
  ❌ @storybook/addon-a11y        ← 커리큘럼 실습 「a11y 자동 검사」가 이것
  ✅ @storybook/addon-docs
  ▲ Storybook setup completed, but some non-blocking errors occurred.
  종료 코드: 0

그런데도 만들어진 것:
  .storybook/main.ts      (addons 4개가 전부 적혀 있다 — 설치 안 된 것 포함)
  .storybook/preview.tsx
  src/stories/{Button,Page,Header}.stories.ts
  package.json 에 스크립트 2개 + devDeps 10줄

만들어지지 '않은' 것:
  .storybook/vitest.setup.ts   없음
  vitest.config.ts             무변경 (= 「스토리 하나 = 테스트」 배선이 통째로 없다)
  a11y 설정                    없음

=> 「빌드 초록인데 앱이 죽어 있다」 계열의 5번째. G-5(JS 청크 0개)·H-7(앱 전멸)·
   H-8(MISSING_MESSAGE)·H-9(shared 빼면 화면 전멸)에 이어 이번엔 '설치 도구' 가 그렇다.
   main.ts 는 설치되지도 않은 애드온 이름을 적어두므로 파일만 보면 성공처럼 보인다.

================================================================
5절. 🚨🚨 실패 원인을 통제된 변수로 갈랐다
================================================================
npm 에러: `Cannot read properties of null (reading 'edgesOut')`
스택: @npmcli/arborist build-ideal-tree.js #loadPeerSet (npm 11.5.2 / node v23.6.0)
크래시 직전 해석 대상: @vitest/browser@3.2.7 → webdriverio@^7||^8||^9 → @vitest/ui@3.2.7

가설 검증 (매번 npm install 재실행):
  (a) 워크스페이스 안에서 install 해서?      → 루트에서 해도 동일 실패. 기각
  (b) init 이 박은 "latest" 문자열 2개?      → 실제 버전으로 고정해도 동일 실패. 기각
      (@chromatic-com/storybook: latest, playwright: latest)
  (c) @storybook/addon-vitest 가 원인?       → 빼도 동일 실패. 기각
  (d) @vitest/browser + browser-playwright?  → 둘 다 빼면 ✅ 설치 성공
  (e) @vitest/browser 만 ^4.1.10 로 복귀?    → 실패 재현. ← 범인 확정
  (f) @vitest/browser 를 정확히 4.1.10 으로? → ✅ 성공

근본 원인:
  @vitest/browser 는 vitest 를 '정확한 버전' 으로 peer 고정한다.
      @vitest/browser@4.1.11 → peer vitest "4.1.11"   (캐럿 없음)
      @vitest/browser@4.1.10 → peer vitest "4.1.10"
  우리 vitest 는 4.1.10 인데 "^4.1.10" 은 4.1.11 로 해석된다 → peer 충돌 →
  npm 이 버전을 거슬러 내려가다 3.x 대에 닿고, 그 시절 peer(webdriverio)를 풀다가
  ERESOLVE 를 내는 대신 널 참조로 죽는다.

해법: @vitest/browser 와 @vitest/browser-playwright 를 설치된 vitest 와 '정확히' 맞춘다.
      "@vitest/browser": "4.1.10"
      "@vitest/browser-playwright": "4.1.10"

⚠️ H-4·H-6 canon 이 "vitest 4.1.10 / registry 4.1.11 — patch 뒤짐, 교안이 인용 안 해 무관"
   이라고 넘겼던 그 한 칸이 여기서 설치를 막는다. 「인용 안 하니 무관」이 언제 무관이
   아니게 되는지의 실례.

================================================================
6절. 배선을 손으로 마저 채웠다
================================================================
설치가 끝난 뒤 postinstall 을 다시 돌렸다:
  (main.ts 의 addon-vitest 줄을 임시로 지워 프롬프트를 없앤 뒤)
  npx storybook add @storybook/addon-vitest --skip-install
  => "Updating your Vitest config file" + "a11y addon setup successfully" + Done!

vitest.config.ts 가 projects 2개로 재구성됨:
  [0] 기존 jsdom 프로젝트 — environment/globals/setupFiles/exclude(e2e) '전부 보존'
      (우리가 쓴 한국어 주석도 그대로 살아남았다)
  [1] name:'storybook' — storybookTest({configDir}) + browser.provider playwright chromium headless

preview.tsx 에 a11y 설정이 붙었다:
  a11y: { test: 'todo' }
  주석 원문: 'todo' - show a11y violations in the test UI only
             'error' - fail CI on a11y violations
             'off' - skip a11y checks entirely
⚠️ 기본값 'todo' 는 '아무것도 실패시키지 않는다'. 커리큘럼의 「a11y 자동 검사」는
   'error' 로 바꿔야 성립한다. 「침묵은 통과가 아니다」 게이트 캐논의 재현.

================================================================
7절. 스토리가 기존 테스트 실행에 그대로 합류한다
================================================================
배선 후 npm test:  155 files / 1069 passed   (+3 파일 / +8 판)
늘어난 3개는 init 이 만든 예제 스토리(Button/Page/Header.stories.ts).

=> 별도 명령(npx vitest --project=storybook)을 안 불러도 기존 `npm test` 가 브라우저
   프로젝트를 함께 돈다. F-5 의 「.spec.ts 가 두 러너에 걸린다」의 속편인데 방향이 반대다 —
   그때는 '막아야' 했고 이번엔 '저절로 합류' 한다.
   기준선을 이 회차부터 다시 잡아야 한다.

================================================================
8절. 아직 안 재본 것 (집필 전 반드시)
================================================================
(1) F-5 마무리 예고의 진위:
    "지난 시간에 만든 목록이 거기서도 한 번 더 쓰입니다" (F5-lecture.md:963)
    = MSW 핸들러(src/mocks/handlers.ts)가 Storybook 에서도 쓰인다는 약속.
    그런데 커리큘럼이 지목한 3개(PostCard·Avatar·CommentInput)는 전부 props 로만
    그려져 네트워크를 안 탄다. 예고가 거짓일 가능성이 크다.
    => 실행으로 확인하고, 거짓이면 F-5 예고를 고칠지 F-6 에서 무대를 만들지 정한다.
       (캐논: 「우리 예고도 원본 예제와 똑같이 실행으로 검증한다」)

(2) preview.tsx 가 globals.css 를 import 하지 않는다
    => 카탈로그가 우리 Tailwind 토큰 없이 그려질 것. E-1~E-7 의 작업이 통째로 안 보인다.
       실제로 확인하고 Step 으로 세울지 정한다.

(3) CommentInput 은 controlled 필수 props 3개(value/onChange/ref)
    => 카탈로그에 혼자 못 선다. 스토리에서 상태를 쥐어줘야 한다.

(4) getByRole('banner') = 0 (F-5 미해결)
    원인은 이번에 확인됨: Layout.tsx 의 <header> 가 <main> 안에 있다.
    header 는 main/article/section/aside/nav 의 자손이면 banner 역할을 안 갖는다.
    => axe-core 가 이것을 잡는지 여부는 아직 안 쟀다.

(5) Chromatic 은 계정이 필요하다 — H-1 의 「계정 없는 설계」 선례를 따를지 정한다.

================================================================
9절. Step 2 실측 — 카탈로그는 우리 스타일을 안 물려받는다
================================================================
첫 스토리: src/components/ui/avatar.stories.tsx (Default/Small/Large/NoImage)
앱이 실제로 쓰는 것은 shadcn ui/avatar 다 (PostHeader.tsx:3).
src/components/Avatar.tsx(B-3 시절)는 테스트 3개만 참조하고 앱은 안 쓴다.

storybook dev -p 6006 → 200. index.json 에 스토리 4개 등록 확인.
⚠️ title 을 한글('카탈로그/Avatar')로 주면 스토리 id 가 퍼센트 인코딩된다
   ('%EC%B9%B4%ED%83%88%EB%A1%9C%EA%B7%B8-avatar--default'). 동작에는 문제 없음.

[통제된 before/after — globals.css import 한 줄]
같은 스토리, 같은 브라우저, 그 한 줄만 넣고 새로고침:

                        없을 때        있을 때
  display               inline         flex
  width / height        auto / auto    32px / 32px
  border-radius         0px            완전 둥글게
  <img> 계산 폭         64px           32px      ← 원본 크기 그대로 나오고 있었다
  Tailwind 규칙 존재    없음           있음
  인라인 <style> 개수   3              4  (+53,729자)

=> 카탈로그가 컴포넌트를 그리기는 하는데 E-1~E-7 에서 쌓은 토큰·유틸리티가
   하나도 안 걸려 있었다. 「그려진다」와 「제대로 보인다」는 다른 것.
   preview.tsx 는 앱의 index.html 을 안 쓰고 자기 화면을 따로 그린다.

[크기 3종 실측 — args 가 실제로 먹는지 확인]
  Small   data-size="sm"       24px   (size-6)
  Default data-size="default"  32px   (size-8)
  Large   data-size="lg"       40px   (size-10)
  => 소스의 클래스와 정확히 일치.

[NoImage 스토리]
  <img> 가 DOM 에 아예 없다(imgInDom: false) · fallback "민지" 만 보인다 · 14px.
  E-6 canon(「들여온 Avatar 는 사진이 오기 전 <img> 를 안 그린다」)의 재현.
  => 앱에서 이 상태를 보려면 네트워크를 끊어야 하는데 카탈로그에선 한 줄이면 된다.
     이것이 「스토리 하나 = 문서」의 값.

[번들 영향]
  index-*.js 683.01 kB / gzip 219,470 B (예산 225,000) · CSS 44.00 kB
  => 스토리 파일은 앱 엔트리가 import 하지 않아 배포본에 안 실린다
     (C-4 「데모 파일은 번들에 0 B」 계열).

================================================================
10절. Step 3~4 실측
================================================================
[Step 3 — 필수 props 가 있는 컴포넌트는 타입이 먼저 막는다]
CommentInput 은 value·onChange·ref 셋이 필수다.
render 만 적고 args 를 비우면:
  TS2322: Property 'args' is missing in type '{ render: () => Element; }'
          but required in type '{ args: { value; onChange; ref } }'
=> 카탈로그도 그 약속을 채우지 않고는 컴포넌트를 못 세운다.
   meta.args 로 약속을 채우고 실제 값은 StatefulCommentInput 이 쥔다.
   브라우저에서 직접 입력해 확인 — 빈 칸 → '카탈로그에서 직접 쳐본 글자' 반영됨.

[Step 4 — 여섯 변형 실측(브라우저 계산값)]
  default      bg oklch(0.205 0 0)              32px
  outline      bg oklch(1 0 0)                  32px
  secondary    bg oklch(0.97 0 0)               32px
  ghost        bg transparent                   32px
  destructive  bg oklab(0.641 … / 0.1)          32px
  link         bg transparent                   32px
⚠️ default 가 우리 브랜드색(#0095f6)이 아니라 shadcn 중립색이다.
   E-7 이 --ring·--destructive 는 우리 토큰으로 이었지만 --primary 는 안 이었다.

⚠️⚠️ 앱에 Button 이 두 개다 — 카탈로그를 세우니 드러났다.
   ui/button(cva 변형): ConfirmDialog·SignInButton·PostDetailPage·ExplorePage·DmPage (5)
   components/Button(B-3, 자기 생김새 없음): PostBody·CommentForm·IconButton·
     SignUpForm·InputValueDemo·RefVsStateDemo·ProfileImagePicker (7)

================================================================
11절. Step 5 — 스토리가 판이 된다 + 🚨 재현 안 되는 실패 1건
================================================================
vitest.config.ts 를 projects 2개로. 첫 실행:
  Test Files  2 failed | 153 passed (155)
  Tests      10 failed | 1063 passed (1073)
  TypeError: Failed to fetch dynamically imported module:
    http://localhost:63315/node_modules/.cache/storybook/10.5.10/<해시>/
    sb-vitest/deps/@storybook_react-dom-shim.js?v=39798587

재현 시도 2회 — 둘 다 실패가 '안' 났다:
  (a) storybook dev 를 끄고 재실행           → 155 / 1073 통과
  (b) dev 를 다시 켜고 재실행                → 155 / 1073 통과  ← 가설 기각
  (c) .cache/storybook(37M) 통째로 지우고 dev 끈 채 → 155 / 1073 통과 ← 가설 기각

=> 처음에 「dev 서버가 캐시를 두고 다툰다」고 결론 낼 뻔했는데 (b) 가 반증했다.
   차가운 캐시 가설도 (c) 가 반증했다.
   현재로선 '첫 실행에서 한 번' 나는 경합으로 보이고 조건을 특정하지 못했다.
   ⇒ 교안은 원인을 단정하지 않는다. 「한 번 이렇게 실패할 수 있고 다시 돌리면
      통과한다 · 우리가 두 조건으로 재현을 시도했지만 안 됐다」로만 쓴다.
      (캐논: 재현 안 되는 수치는 인용 못 하지만 「재현이 안 된다」는 쓸 수 있다)

[안정 상태 — 3회 연속 동일]
  Test Files  155 passed (155)      (152 + 스토리 파일 3)
  Tests      1073 passed (1073)     (1061 + 스토리 12)
  스토리 12 = Avatar 4 + CommentInput 2 + Button 6
  => 이 회차부터 기준선은 155 / 1073.

⚠️ storybook/test 가 내보내는 것이 F-3 에서 배운 그 목록이다 —
   expect · userEvent · within · screen · getByRole · findByRole · fn …
   (@storybook/test 라는 패키지는 이제 없다. storybook/test 로 들어온다)

================================================================
12절. Step 6 — play 함수는 진짜로 무언가를 지킨다
================================================================
storybook/test 가 F-3 의 그 목록을 그대로 준다
(expect·userEvent·within·screen·getByRole·findByRole·fn).
jest-dom 매처도 온다 — toBeDisabled·toBeEnabled·toHaveValue·toBeInTheDocument
전부 tsc 통과 + 런타임 통과(타입 선언 파일에는 이름이 안 보이는데 실제로는 붙어 있다).

156 files / 1076 passed (155/1073 → +1 파일 +3 판)

[가드를 일부러 깨서 확인 — 이 회차 최고의 교보재]
useCommentInput 의 `content.trim() === ''` 를 `content === ''` 로 바꾸니 4건 FAIL:
  |storybook| CommentForm.stories.tsx > Whitespace Only     ← 오늘 만든 스토리
  |unit|      b2-form.test.tsx > 공백만 쳐서는 …             (B-2)
  |unit|      f3-comment-form.test.tsx > 공백만 넣으면 …     (F-3)
  |unit|      b3-use-comment-input.test.tsx > 빈 입력은 …    (B-3)
=> 스토리가 기존 판들과 '같은 결함' 을 같이 잡는다. 장식이 아니다.
   되돌리니 156/1076 복귀.

================================================================
13절. Step 7 — 'error' 로 바꾸니 5건이 나왔고 전부 진짜였다
================================================================
preview.tsx 의 a11y test 를 'todo'(도구 기본값) → 'error' 로.

  FAIL  avatar.stories.tsx > Default / Large / Small / No Image
        $('.size-full') — color-contrast
  FAIL  button.stories.tsx > All Variants
        $('.bg-destructive\/10') — color-contrast

[대비 실측 — OKLCH→sRGB→WCAG 로 계산]
  Avatar fallback 밝은 화면   배경 #f5f5f5 / 글자 #737373  → 4.34 : 1  (기준 4.5)
  Avatar fallback 어두운 화면 배경 #262626 / 글자 #a1a1a1  → 5.83 : 1  통과
  destructive 버튼 밝은 화면  배경 #fdedee / 글자 #ed4956  → 3.26 : 1
    ⚠️ E-7 이 만든 --color-danger-strong(0.58)로 글자만 바꿔도 4.18 — 여전히 미달.
       배경이 '같은 빨강의 10%' 라 글자가 훨씬 어두워져야 한다.
       E-7 의 「한 값으로 두 화면 불가」가 여기선 「한 값으로 두 조합 불가」로 나온다.

[처리 — 사용자 결정]
(1) Avatar 는 고친다: --muted-foreground 0.556 → 0.54  ⇒ 4.64 : 1
    사진이 안 뜬 아바타의 이름 글자라 사용자가 실제로 보는 화면이다.
    ⚠️ 이 값은 들여온 shadcn 기본값이었다(E-5 때 같이 들어왔다). 우리가 고른 게 아니다.
    계산된 색을 단언하는 판이 0건이라 다른 회차 무영향(grep 으로 확인).
(2) destructive 는 안 고친다: 앱이 이 변형을 '한 군데도 안 쓴다'(grep 0건).
    카탈로그가 여섯을 늘어놓으니까 드러난 것이지 사용자가 보는 화면이 아니다.
    그 스토리만 parameters.a11y.test = 'todo' 로 표시.
    => 'todo' 를 기본값으로 깔면 아무도 안 고치지만, 골라서 쓰면
       「알고 있고 아직 안 고쳤다」는 기록이 된다. 쓰기 시작하는 날 갚아야 한다.

결과: 156 files / 1076 passed · CSS 44.00 kB · 번들 gzip 219,470 B (전부 무변경)

================================================================
14절. Step 8 — 목록을 다시 쓴다 (F-5 예고 상환) · 함정 4연속
================================================================
F-5 마무리(F5-lecture.md:963)의 약속:
  "다음 시간에는 컴포넌트를 카탈로그로 만듭니다.
   지난 시간에 만든 목록이 거기서도 한 번 더 쓰입니다."
=> 지킬 수 있다. 단 커리큘럼이 지목한 셋(PostCard·Avatar·CommentInput)은
   전부 props 로만 그려져 네트워크를 안 탄다. 목록이 필요한 무대는 HomePage 다
   (useFeedQuery 로 가져오고 하트를 누르면 useLikeMutation 이 나간다).

[함정 1 — msw-storybook-addon 3.0 은 2.x 와 API 가 통째로 다르다]
  검색에 나오는 예제: import { initialize, mswLoader } from 'msw-storybook-addon'
  3.0 실물: index.mjs 는 `addonMsw` 를 default 로만 내보낸다. initialize 는 없다.
  오류: "does not provide an export named 'initialize'"
  => C-1 react-router-dom · D-2 next/router · E-7 MoreHorizontal 과 같은 계열.

[함정 2 — 애드온은 흉내 서버를 '켜주기만' 하고 목록은 안 읽는다]
  addon-DN-DOgIS.mjs 의 createPreviewAnnotations 실물:
      beforeEach → setup() 으로 worker 를 만들고 context.msw 에 얹고
                   끝나면 resetHandlers() 만 한다.
  defaultSetup 은 `setupWorker()` — 인자 없이 부른다(= 핸들러 0개).
  parameters.msw.handlers 를 읽는 코드가 '없다'.
  그건 csf3.mjs 의 mswLoader 쪽에만 있고 그쪽은 deprecated 경고를 낸다.
  => 우리는 F-4 가 만든 worker(src/mocks/browser.ts)를 setup 으로 그대로 건넸다.
     setupWorker(...handlers) 로 이미 목록을 품고 있는 그 객체다.
     "목록이 한 번 더 쓰인다" 의 가장 강한 형태.

[🚨 함정 3 — addons 가 조용히 무시된다]
  preview.tsx 가 그냥 객체를 export default 하면 addons 키가 '아무 일도 안 한다'.
  tsc 통과 · 오류 0 · 경고 0 · 서비스 워커 등록 0건.
  실측: navigator.serviceWorker.getRegistrations() → [] · controller → null
  definePreview({...})(@storybook/react-vite) 로 감싸니 즉시:
    등록 1건 scope http://localhost:6006/ · state activated
    controller http://localhost:6006/mockServiceWorker.js · 카드 10장
  => 「타입은 통과하는데 동작은 안 한다」 계열(D-3·C-7·C-4·H-8).
     이 회차에서 가장 조용한 실패다.

[함정 4 — 내 단언이 틀렸지 코드가 틀린 게 아니었다]
  getByText('오늘 한강 노을이 미쳤다') 실패.
  실제 화면: "jaehoon 오늘 한강 노을이...더 보기" — E-7 의 캡션 접기 때문에
  그 문자열은 통째로 그려지지 않는다.
  => D-1 canon(「한 표현식이 통째로 만든 문자열을 골라라」)의 재현.
     findAllByRole('article') 10장 + '좋아요 1240개' 로 바꿨다.

[🚨 흉내 서버가 두 곳에서 다른 말을 하고 있었다]
  하트 클릭 → POST /api/posts/1/like 200 → GET /api/posts 재요청 200
  그런데 숫자가 1240 그대로.
  원인: 좋아요 핸들러는 likeState 에 기억하는데 GET /posts 핸들러는
        allPosts 를 '그대로' 돌려줬다. 다시 물어보는 순간 옛 값으로 덮인다.
  고침: GET /posts 가 likeState 를 얹어서 준다.
  => 흉내 서버도 두 창구가 같은 사실을 말해야 한다.
     이건 F-4 의 「핸들러는 사실이 아니라 우리의 주장이다」의 직접적인 연장.

[Loading 이야기를 결정적으로 만들기]
  흉내 서버가 100ms 만에 답해서 '불러오는 중' 을 놓칠 수 있다.
  그 이야기에서만 msw.use() 로 delay('infinite') 를 씌웠다
  — F-4 의 server.use() 가 카탈로그에서 그대로 재현되는 자리.

================================================================
15절. 과제를 직접 풀었다 (D-4 이후 29연속) — 지문이 두 번 바뀌었다
================================================================
[과제1 — PostCard] 값이 '열 개' 다(데이터 9 + onToggleLike).
  교안 초안이 "아홉 개" 라고 적었다가 tsc 로 세어 정정했다.
  ⚠️ 값을 열 개 다 줘도 안 선다:
     useLocation() may be used only in the context of a <Router> component.
     ❯ useSearchParams src/components/PostBody.tsx:39
  C-4 에서 「읽는 쪽을 PostBody 로 내렸다」가 여기서 대가를 드러낸다.
  밖에서 보면 props 만 받는 컴포넌트인데 아니다 → MemoryRouter 필요.
  mediaKind 를 빼면 TS2322 가 '세 이야기 모두' 에 뜬다(meta.args 에 모아둔 결과).

[과제2 — 실패하는 피드] 500 을 주면 화면에 남는 글자:
     "피드를 불러오는 중이에요…"   (영원히 · article 0개)
  오류 화면이 아니다. HomePage 가 `if (!isSuccess) return 불러오는 중` 두 갈래뿐이라
  실패가 로딩으로 보인다. C-7 이 「실패는 위로 던진다」로 갈래를 줄인 대가이고,
  카탈로그에는 받아줄 쪽이 없다.
  => 과제 지문의 "적을 것이 마땅치 않다면 그게 답" 이 실제로 성립한다.
  확인 후 그 이야기는 코드베이스에서 걷어냈다(학생이 쓸 몫).

[과제3 — 접근성 + banner] 🚨 이 회차 마지막 발견
  껍데기는 ThemeProvider 없이는 아예 던진다(C-3 의 던지는 훅이 그대로 걸린다).
  감싸고 나면 axe 위반 0건 — 머리말 문제를 '안 잡는다'.
  위반이 아니라 마크업 구조 선택이기 때문.
  ⚠️⚠️ 그런데 role 개수가 지난 회차와 다르다:
       Playwright (F-5)        banner 0
       Testing Library (F-6)   banner 1     ← 실측
  구조는 동일하다(`main > header` 존재를 단언으로 확인).
  HTML 규칙은 header 가 main 안이면 banner 를 잃는다고 정하고
  Playwright 는 그 규칙을 구현했고 Testing Library 는 안 했다.
  => 「같은 화면을 두 도구가 다르게 본다」. F-5 의 미해결이 '틀린 것' 이 아니라
     '도구에 따라 답이 갈리는 상태' 였다는 것이 이번에 드러났다.

[과제4 — 두 프로젝트 시간] 3회 측정, 편차 거의 없음
       unit        16.46 / 16.51 / 16.53 초   1062판  → 15.5ms/판
       storybook    5.13 /  5.13 /  5.14 초     22판  → 233ms/판   (약 15배)
  ⚠️ 이 나눗셈은 오해를 부른다 — storybook 쪽 5.13초의 대부분이 브라우저 띄우기와
     흉내 서버 시작(고정비)이고 판은 22개뿐이다. 판을 열 배로 늘려도 열 배 안 된다.
  F-5 의 170배와 갈리는 이유도 같다(그쪽은 판마다 고정비를 다시 냈다).

================================================================
16절. 게이트 최종
================================================================
npm test        159 files / 1084 passed
                (Step 1~8 만 = 157/1080 · 과제 답안 2파일 4판이 더해진 값)
  unit       152 files / 1062
  storybook    7 files /   22
tsc 0 · eslint 0 · build exit 0 · gzip 219,471 B / 예산 225,000 B · CSS 44.00 kB
grep-check F6  대상 2파일 · FAIL 0 / WARN 0
교안 1,005줄 · 답안 438줄(배점 25×4=100 · 합계행 4)
마감 요소  8 Step / 한 줄 정리 8 / 🙋 토글 8 / 과제 4 / 주제 3   (D-4~H-9 패턴 유지)
tic 0.05% / 0.18% · italic 0 / 0 · 이모지 4종(+❌ 상태) / 3종
250자 초과 0 / 0 · em-dash 3+ 0 · 최장 코드블록 36줄 / 44줄
닫힌 ASCII 박스 폭 불일치 0 · 한 슬래시 주석 0 · 도구 태그 누출 0
코드 블록 대조 23개 중 19개 축자 일치(나머지 4개는 의도된 것 —
  init 산출물·타입이 막는 형태·일부러 깬 코드·도구 기본값)

⚠️ 11절의 「재현 안 되는 실패」에 조건이 하나 더 붙었다.
   마지막에 같은 실패가 재현됐고 vitest 가 원인을 직접 말했다:
     [vitest] Vite unexpectedly reloaded a test.
   정확한 조건은 「dev 서버가 떠 있다」가 아니라
   「dev 서버가 떠 있는 채로 파일이 바뀌어 공유 캐시를 다시 최적화한다」이다.
   dev 서버만 켜두고 아무것도 안 고치면 초록(2회 확인),
   고치는 중에 돌리면 빨갛다. 교안은 이 조건까지 적지 않았다 —
   학생이 재현할 만한 상황이 아니라서 🙋 로도 안 넣었다.
