E-2 @theme 토큰 관찰 (Tailwind v4.3.3 · Vite 8.1.5)
====================================================

단위 테스트로 잡을 수 없는 사실을 기록한다.
빌드 산출 CSS 계측 + 브라우저 computed style / canvas 픽셀 판독.

재현 (리포지토리 루트에서):
  npm run build --workspace apps/web-spa
  python3 -c "import glob,os; print(len(open(sorted(glob.glob('apps/web-spa/dist/assets/*.css'), key=os.path.getmtime)[-1]).read()))"

색 비교는 문자열이 아니라 픽셀로 재야 한다. getComputedStyle 도 canvas fillStyle 도
oklch() 를 그대로 직렬화해서 돌려주므로 문자열 비교는 아무것도 증명하지 못한다.
canvas 에 칠하고 getImageData 로 읽은 값이 학생이 실제로 보는 색이다.


1. 선언만 하면 아무 일도 안 일어난다
--------------------------------------

  @theme 에 색 토큰 9개를 선언하고 아무 컴포넌트에서도 쓰지 않은 상태:

    CSS 산출 9,221 B  <- 선언 전과 byte 완전 동일
    :root 에 우리 토큰: 없음
    생성된 유틸리티:    없음

  E-1 의 "안 쓴 유틸리티는 애초에 안 생긴다" 가 토큰에도 그대로 적용된다.
  토큰 선언은 "이런 이름을 쓸 수 있게 해달라" 는 등록이고, CSS 는 쓸 때 나온다.

  9개를 전부 쓰면:
    CSS 산출 9,812 B (+591)
    :root 에 우리 토큰 9개 전부 출력
    유틸리티 9개 생성 (.bg-brand .text-danger .border-line .border-line-soft
                       .text-muted .text-subtle .text-ink .bg-canvas .bg-surface)

  ※ #ffffff 는 산출물에서 #fff 로 최소화된다 (Lightning CSS).


2. @theme 과 :root 의 차이 — 두 가지다
----------------------------------------

  대조군: :root { --color-probe-root: #ff00ff; } 를 넣고 bg-probe-root 를 써봤다.

    --color-probe-root  변수로는 나온다 (#f0f)
    .bg-probe-root      X 안 생긴다

  반면 @theme 의 --color-brand 는 변수도 나오고 .bg-brand 도 생긴다.

  차이 (1) 유틸리티를 만드는가 — @theme 은 만들고, :root 는 안 만든다.
  차이 (2) 언제 출력되는가   — @theme 은 쓴 것만, :root 는 아무도 안 써도 무조건.
                              (:root 한 줄 추가로 9,812 -> 9,842 B, +30 B)

  => :root 는 "CSS 변수를 만드는 곳", @theme 은 "Tailwind 에게 값을 알려주는 곳".


3. Tailwind 기본 팔레트는 실제로 OKLCH 다
------------------------------------------

  --color-neutral-50   oklch(98.5% 0 none)
  --color-neutral-300  oklch(87% 0 none)
  --color-neutral-500  oklch(55.6% 0 none)
  --color-neutral-800  oklch(26.9% 0 none)

  단 --color-white 와 --color-black 은 #fff / #000 로 hex 다.

  ⚠️ 위 값들은 "아무도 안 쓰면 출력되지 않는다"(1번). 그래서 CSS 에서 찾으려면
     먼저 bg-neutral-500 같은 걸 어디서든 써야 한다. 이걸 모르고 grep 하면
     "팔레트가 없다" 는 잘못된 결론이 나온다.


4. hex -> OKLCH 변환은 픽셀까지 같다. 단 정확히 변환했을 때만.
---------------------------------------------------------------

  우리 9색을 CSS Color 4 공식으로 변환하고 canvas 픽셀로 대조:

    이름          원래 값    OKLCH                        픽셀
    brand         #0095f6    oklch(65.46% 0.177 248.2)    0,149,246    동일
    danger        #ed4956    oklch(64.1% 0.2 20.5)        237,73,86    동일
    line          #dbdbdb    oklch(89.1% 0 none)          219,219,219  동일
    line-soft     #efefef    oklch(95.2% 0 none)          239,239,239  동일
    muted         #8e8e8e    oklch(64.7% 0 none)          142,142,142  동일
    subtle        #737373    oklch(55.6% 0 none)          115,115,115  동일
    ink           #262626    oklch(26.9% 0 none)          38,38,38     동일
    canvas        #fafafa    oklch(98.5% 0 none)          250,250,250  동일
    surface       #ffffff    oklch(100% 0 none)           255,255,255  동일

  9색 전부 delta 0. 위 정밀도(L 소수 1~2자리·C 3자리·H 1자리)가 픽셀이 정확히
  일치하는 최소 표기다. 더 줄이면 틀어진다.

  ⚠️⚠️ 처음에 눈대중으로 적은 OKLCH 값은 눈에 보이게 틀어졌다.
     brand  0,149,246 -> 0,132,236   (파랑 10, 초록 17 차이)
     danger 237,73,86 -> 233,61,82
     line   219,219,219 -> 216,216,216
     "대충 이 정도" 로 적으면 안 된다. 반드시 변환해서 넣고 픽셀로 확인할 것.

  ⚠️ 우리 회색 3개는 OKLCH 로 적으면 Tailwind 기본 neutral 과 문자열까지 같다.
     subtle  oklch(55.6% 0 none) == neutral-500
     ink     oklch(26.9% 0 none) == neutral-800
     canvas  oklch(98.5% 0 none) == neutral-50
     line 은 다르다 (89.1% vs neutral-300 의 87%).
     => "Tailwind 가 우리 색을 모른다" 는 brand·danger·line 에 대해서는 참이지만
        회색 세 개에 대해서는 거짓이다. 이름을 주는 이유는 값이 없어서가 아니라
        용도를 부르기 위해서다(그리고 다크모드에서 값만 갈아끼우기 위해서).


5. 눈금 — 우리 값은 대부분 이미 안에 있었다
--------------------------------------------

  간격: --spacing 은 연속 배수라 0.25rem(4px) 단위면 뭐든 된다.
        p-2.5 -> calc(var(--spacing) * 2.5) = 10px
        p-4.5 -> calc(var(--spacing) * 4.5) = 18px
        우리 간격 4/8/10/12/16/18/24/32 전부 눈금 안. 토큰 필요 없음.

  글꼴: --text-xs .75rem(12) · --text-sm .875rem(14) · --text-lg 1.125rem(18)
        --text-2xl 1.5rem(24)
        우리 값 12·14·18·24 전부 눈금 안. 13px 만 벗어난다 (6곳에 반복).

  모서리: --radius-md .375rem(6) · --radius-lg .5rem(8)
        우리 값 6·8 눈금 안. 20px 만 벗어난다 (1곳).

  => 이름을 줄 기준은 "눈금에서 벗어났는가" + "몇 번 반복되는가" 두 개다.
     13px 은 6회 -> --text-note 로 이름을 줬다.
     20px radius 와 470px max-width 는 1회씩 -> 대괄호로 그 자리에서 넘긴다.

  ⚠️ E-1 답안이 18px 을 "눈금에 없는 값이라 대괄호" 라고 적은 것은 틀렸다.
     --text-lg 가 1.125rem = 18px 이다. text-lg leading-none 을 재보면
     18px / 18px 로 손 CSS(font-size 18px; line-height 1)와 정확히 같다.
     (text-[18px] leading-none 도 같은 값이라 화면 결과는 맞았고, 이유만 틀렸다.)
     => E1-answers.md 를 정정했다.


6. ⚠️ text-* 는 줄 높이를 함께 정한다 — 손 CSS 는 안 정했다
-------------------------------------------------------------

  브라우저 computed (root font-size 16px):

    값        손 CSS (line-height 미지정)   유틸리티
    12px      12px / 18px                  text-xs   12px / 16px   다름
    13px      13px / 19.5px                text-note 13px / 19.5px 같음
    14px      14px / 21px                  text-sm   14px / 20px   다름
    18px + 1  18px / 18px                  text-lg leading-none
                                                     18px / 18px   같음
    24px      24px / 36px                  text-2xl  24px / 32px   다름

  손 CSS 는 line-height 를 한 번도 정하지 않아 브라우저 기본 normal(약 1.5배)에
  기대고 있었다. text-xs/sm/2xl 로 옮기면 Tailwind 가 정한 줄 높이가 따라온다.
  E-1 의 Preflight 교훈과 같은 모양이다 — 기본값에 기대던 곳만 드러난다.

  ⚠️ 임의 값(text-[13px])과 우리가 만든 --text-note 는 줄 높이를 안 정한다.
     named 눈금(text-sm 등)에만 --text-*--line-height 짝이 있기 때문이다.
     --text-note--line-height 를 만들지 않았으므로 text-note 는 font-size 만 정한다.
     => 그래서 13px 자리는 옮겨도 줄 높이가 안 바뀐다.

  이 모듈은 "Tailwind 가 정한 줄 높이를 따른다" 로 결정했다.
  그래서 12/14/24px 자리의 줄 높이는 의도적으로 바뀐다. 픽셀이 그대로라고
  쓰면 거짓이다.


7. 피드를 옮긴 뒤 계측 (Step 5 종료 시점)
------------------------------------------

  CSS 산출  9,221 -> 9,713 B (+492)
  globals.css  373 -> 264 줄 · 손 클래스 규칙 47 -> 34 개 (19 블록 삭제)

  ⚠️ 손 규칙 19개를 지웠는데 CSS 가 오히려 492 B 늘었다.
     유틸리티 하나하나가 클래스 이름 + 선언을 따로 갖기 때문이다.
     E-1 의 "안 쓴 유틸리티는 안 생긴다" 를 "Tailwind 를 쓰면 CSS 가 작아진다" 로
     읽으면 안 된다. 줄어드는 것은 우리가 관리하는 CSS 파일이고,
     산출 CSS 는 늘 수도 있다.

  ⚠️ :root 변수 9 -> 22 개 (+13). 그런데 우리 토큰은 7개뿐이다.
     우리 것    --color-danger/line/muted/ink/canvas/surface · --text-note   (7)
     Tailwind 것 --text-lg(+--line-height) · --text-2xl(+--line-height)
                --radius-md · --radius-lg                                   (6)
     text-lg·text-2xl·rounded-md·rounded-lg 를 쓰면서 Tailwind 자기 눈금 값이
     함께 딸려 나온 것이다. "우리 토큰 때문에 13개 늘었다" 고 쓰면 거짓이다.

  ⚠️ 선언한 색 9개 중 6개만 :root 에 나온다.
     안 나온 것: brand · line-soft · subtle
     세 개가 쓰이는 곳(.comment-submit·.signup-submit·.comment-form·.signup-label
     ·.profile-picker-name·.input-demo p·.response-verdict)이 아직 손 CSS 라서다.
     1번 규칙이 Step 5 시점에도 그대로 성립한다.


8. 데모 컴포넌트가 피드 클래스를 함께 쓰고 있었다
--------------------------------------------------

  피드 규칙을 지우자 아래가 함께 스타일을 잃어 같이 옮겨야 했다.

    MiniPostCard     post-card · post-content · post-likes  (변형 2개)
    StaticPostCard   post-card · post-image · post-likes · post-content
    KeyDemo          like-button(+liked) · hide-button ×2
    ClickCounter     like-button

  E-1 Step 1 이 말한 "이름을 공유하면 한 곳을 고칠 때 다른 곳이 흔들린다" 가
  실제로 일어난 자리다. 교안 Step 5 에서 이 연쇄를 그대로 보여줄 수 있다.

  ⚠️ 이 넷은 App 에 마운트되지 않는다(B-1·B-2 시연용).
     그래서 브라우저에서 눈으로 확인할 수 없고 렌더 테스트로만 검증된다.

  ⚠️ Section 의 class="section" 은 대응하는 CSS 규칙이 아예 없는 죽은 이름이었다.
     함께 걷어냈다. b3-use-like-toggle 의 withoutSignUpSection 정규식이
     이 이름을 찾으므로, 되돌리기를 먼저 적용한 뒤 잘라내도록 순서를 고쳤다.


9. 옮긴 뒤 브라우저 실측 — 값이 그대로인지
--------------------------------------------

  body            bg 250,250,250 · color 38,38,38
                  font-family 에 'Apple SD Gothic Neo' 보존
                  => @theme 으로 --font-sans 를 덮어써서 지켰다.
                     (내장 토큰도 덮어쓸 수 있다 — 새 토큰만 만드는 게 아니다)
  main            max-width 470px · padding 16px · 가운데 정렬
  h1              24px / 700 / margin-bottom 16px   (줄 높이만 36 -> 32)
  header          flex · space-between · baseline · mb 16px
  h2(구역 제목)   14px / 600 / 142,142,142 / mb 12px
  ul(피드 목록)   list-style none · margin 0 · padding 0
                  => 유틸리티 0개. Preflight 가 이미 셋 다 해준다.
  article(카드)   bg 255,255,255 · border 1px solid 219,219,219
                  radius 8px · margin-bottom 24px · overflow hidden
  더보기 버튼     18px · padding 12px · cursor pointer
                  => text-lg leading-none 이 손 CSS 18px/1 과 같다(E-1 답안 정정 근거)
  좋아요 버튼     border 219,219,219 · 1px · radius 6px · padding 6/12px · 14px
    liked         border 237,73,86 · color 237,73,86 · weight 600
  좋아요 개수     padding 12/12/4/12 · 14px · 600
  캡션 접기       14px · 142,142,142 · padding-left 4px

  => 색·간격·굵기·모서리는 전부 같다. 바뀐 것은 글자 크기 유틸리티가
     함께 정하는 줄 높이뿐이다(6번).

  ⚠️ 색 판독 헬퍼 주의: transparent 를 canvas 에 칠하면 0,0,0 으로 읽힌다.
     투명 여부는 픽셀로 재지 말고 계산값 문자열로 확인할 것.


10. Preflight 가 이미 해주는 것 (유틸리티를 안 써도 되는 자리)
---------------------------------------------------------------

  손 CSS 를 그대로 유틸리티로 옮기면 필요 없는 것까지 적게 된다.
  새 요소를 만들어 계산값을 재서 확인했다.

    ul      margin 0 · padding 0 · list-style none
    p       margin 0 · padding 0
    h1~h6   margin 0
    button  margin 0 · padding 0 · background transparent
            border-width 0 · color 상속 · font 상속 · outline none
    img     display block · max-width 100%
    input   border-width 0 · background transparent · outline none

  그래서 아래는 옮길 필요가 없었다.
    .feed-list 의 세 선언 전부
    .post-* 의 margin: 0
    .feed-title·.section-title 의 margin 위쪽
    버튼들의 border: none · background: none · color · padding: 0
    .post-image 의 display: block


11. 댓글·폼·토스트를 옮긴 뒤 (Step 6 종료 시점)
------------------------------------------------

  CSS 산출  9,713 -> 10,212 B
  globals.css  264 -> 167 줄 · 손 규칙 34 -> 22 개
  :root 변수 22 -> 29 개
  선언한 색 9개가 이제 전부 :root 에 나온다
  (brand·line-soft·subtle 이 이 Step 에서 처음 쓰였다)

  남은 22개 = 의사클래스 2개 + 교습용 데모 20개
    .comment-submit:disabled   <- E-3 (disabled: 변형)
    .comment-remove:hover      <- E-3 (hover: 변형)
    .key-demo* .effect-demo* .input-demo* .profile-picker* .response-*  <- 과제


12. ⚠️ 의사클래스 규칙 두 개는 손 이름을 붙잡고 있다 — E-3 복선의 근거
-----------------------------------------------------------------------

  .comment-submit:disabled 와 .comment-remove:hover 는 남겨둬야 해서
  그 두 요소는 손 이름을 className 에 그대로 달고 있다.

    class="comment-submit cursor-pointer text-sm font-semibold text-brand"
    class="comment-remove cursor-pointer px-1 text-sm leading-none text-muted"

  이름을 떼면 규칙이 붙을 자리가 없어진다. 그래서 이 두 곳만 유틸리티로
  끝까지 못 옮긴다 — hover:/disabled: 변형이 있어야 떨어진다(E-3).

  ⚠️ 남겨둔 규칙이 유틸리티를 이긴다 (실측):
    제출 버튼 disabled 상태 -> color 178,223,252 (#b2dffc) · cursor default
    즉 text-brand 유틸리티를 :disabled 규칙이 덮는다.
    이유는 특정도가 아니라 cascade layer 다 — 유틸리티는 @layer utilities 안에
    있고 우리 손 CSS 는 layer 밖이다. layer 밖 스타일이 layer 안보다 강하다.
    (특정도로도 .comment-remove:hover(0,2,0) > .text-muted(0,1,0) 이긴 하다)


13. 토스트 — 유틸리티 표기가 달라도 화면은 같다
------------------------------------------------

    항목        손 CSS                    유틸리티                결과
    배경        rgba(0,0,0,0.82)          bg-black/82            흰 배경 위 46,46,46 동일
    가운데      transform:translateX(-50%) -translate-x-1/2      박스 위치 1249.9 / 폭 60.2 동일
    모서리      border-radius:20px        rounded-[20px]         20px
    padding     10px 18px                 py-2.5 px-4.5          10px / 18px
    글자        14px · #ffffff            text-sm text-white     14px · 255,255,255

  ⚠️ 계산값 문자열은 서로 다르게 나온다. 그래도 화면은 같다.
     배경  bg-black/82 -> oklab(0 0 0 / 0.82) / 손 CSS -> rgba(0, 0, 0, 0.82)
           흰 배경 위에 겹쳐 칠한 픽셀은 둘 다 46,46,46 이다.
     가운데 v4 는 transform 이 아니라 modern translate 속성을 쓴다.
           유틸리티: transform none · translate -50%
           손 CSS:   transform matrix(...) · translate none
           getBoundingClientRect 로 재면 위치와 폭이 같다.
     => 색·정렬은 계산값 문자열로 견주면 안 된다. 픽셀과 박스로 재야 한다.

  ⚠️ 20px 모서리와 470px 폭은 각각 한 번만 쓰여서 이름을 주지 않고
     대괄호로 넘겼다(5번의 기준). 13px 은 6회라 이름을 줬다.


14. 댓글 구역 실측 (옮긴 뒤)
-----------------------------

  제출 버튼 활성   color oklch(0.6546 0.177 248.2) = #0095f6 · cursor pointer
  제출 버튼 잠김   color 178,223,252 · cursor default        (남겨둔 규칙)
  삭제 버튼        color 142,142,142 · 14px · line-height 14px · padding-left 4px
  댓글 목록        padding-left 12px · padding-bottom 12px · 14px


15. 과제 최종 상태 실측 (실제로 만들어보고 되돌렸다)
-----------------------------------------------------

  교습용 데모 20규칙까지 옮기면:

    globals.css     167 -> 35 줄 · 손 규칙 22 -> 2 개
    CSS 산출        10,212 -> 8,958 B
    :root 변수      29개 그대로 (새로 쓴 유틸리티가 이미 있던 토큰만 썼다)
    tsc 0 · eslint 클린 · build 성공 · 58 files / 438 passed

  globals.css 에 남는 것: @import · @source not 2줄 · @theme · 의사클래스 2규칙

  ⚠️⚠️ 네 시점 CSS 크기가 오르다가 내려간다.
      9,221 (E-1 종료) -> 9,713 (Step 5) -> 10,212 (Step 6) -> 8,958 (과제 완료)
      중간에는 손 CSS 와 유틸리티가 함께 있어서 늘고, 손 CSS 가 다 빠지면
      시작점보다 작아진다. "Tailwind 를 쓰면 CSS 가 작아진다" 는
      마이그레이션이 끝난 뒤에만 참이다. Step 5·6 만 보고 결론 내면 거짓이 된다.

  ⚠️ 죽은 클래스가 둘 있었다 — 대응 CSS 규칙이 아예 없는 이름.
     Section 의 class="section" (Step 5 에서 처리)
     RefVsStateDemo 의 class="ref-demo" (과제 범위)

  반복된 유틸리티 조합 4종 (컴포넌트로 묶을 후보 — B-3 기준을 스타일에 적용하는 자리)
    mb-4 rounded-lg border border-line bg-surface p-3       key-demo · effect-demo
    flex-1 rounded-lg border border-line bg-surface p-3     input-demo · response-panel
    cursor-pointer rounded-md border border-line bg-canvas px-3 py-1.5 text-note
                                          effect-demo-button · profile-picker-button
    text-note text-subtle    profile-picker-name · input-demo p · response-verdict

  채증 diff 전문: scratch/e2-homework-answer.txt
