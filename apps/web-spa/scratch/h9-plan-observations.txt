H-9 (마이크로 프론트엔드) — 계획 단계 채증
측정일: 2026-08-22 · 브랜치 module-H9-micro-frontend
모든 탐침은 scratchpad(저장소 밖)에서 수행 — 워킹트리 무변경 확인됨

================================================================
1절. 착수 확인 (포트 · 기준선)
================================================================
포트 3000/8090/4173/5173/9000 — 착수 시 EMPTY(리스너 0)
기준선 web-spa `npm test` 두 번 재서 대조:
  1회차 152 files / 1061 passed (18.87s)
  2회차 152 files / 1061 passed (18.19s)
  → H-8 종료 상태와 일치. 기준선 확정 152/1061
워킹트리 깨끗(git status 빈 출력)

================================================================
2절. 게이트가 이 회차 핵심 주장을 보는가 (탐침)
================================================================
`grep-check.sh H9` (교안 파일 없는 상태)
  출력: "검증 대상 파일이 없습니다." / 종료코드 0

⚠️ H-2·H-8 canon 재현 — 「보고 통과」가 아니라 「볼 게 없어서 통과」.
게이트에 마이크로 프론트엔드 관련 규칙은 0건이다. 즉 이 회차의 핵심 주장
(MF 버전·번들 세금·독립 배포·Next 16 차단)은 게이트가 원리적으로 못 본다.
→ 판정은 전부 실행/빌드 산출물로 한다. 게이트는 톤·금지어만 본다.

================================================================
3절. 시의성 — 🚨 커리큘럼 H-9 행의 「MF 3.0」은 실재하지 않는다
================================================================
커리큘럼 H-9 행: "MF 3.0+Native ESM 동향 1문단"

[npm registry 실측 — C-4 캐논대로 registry 를 정답으로]
  @module-federation/enhanced  latest 2.8.2  (2026-08-19)
  @module-federation/runtime   latest 2.8.2  (2026-08-19)
  @module-federation/vite      latest 1.20.7 (2026-08-13)
  @module-federation/nextjs-mf latest 8.8.73 (2026-08-19)
  @originjs/vite-plugin-federation 1.4.1     (2026-01-04 — 7개월 정체)
  @softarc/native-federation   latest 4.4.1  (2026-08-10)

  enhanced 전체 배포 버전 1,221개 중 3.x = **0건**
  runtime 전체 배포 버전 중 3.x = **0건**

[공식 문서 실측]
  module-federation.io/blog/announcement.html
    → 현행은 2.0. **3.0 언급 0건 · 3.0 로드맵 0건 · Native ESM 언급 0건**
    → 로드맵에 적힌 것은 디버깅 도구 시각화 + SSR/Data Prefetch 고도화

[판정]
  「MF 3.0」을 말하는 것은 SEO 블로그(blog.weskill.org 등)뿐이고
  registry 에도 공식 문서에도 존재하지 않는다.
  → 커리큘럼 행의 전제 절반이 거짓. G-1(Auth.js→Better Auth) 선례와
    같은 계열이지만 방향이 반대다 — 그때는 원안이 낡아서 교체했고,
    이번엔 원안이 **아직 없는 것을 있다고** 적었다.

  단 「Native ESM」쪽은 실재한다:
    - import maps 는 브라우저 표준으로 이미 널리 지원
    - @softarc/native-federation 4.4.1 이 활발히 유지보수 중(2026-08-10)
  → 동향 문단은 "MF 3.0" 이 아니라 **"import maps 기반 Native Federation"**
    으로 쓴다. 「없는 버전을 예고하지 않는다」

[그 밖 시의성 — 드리프트 0]
  vite 8.1.5(코드베이스) / react 19.2.8 / next 16.3.1 — H-8 확인 그대로

================================================================
4절. MF 2.0 이 우리 스택에서 실제로 도는가 (실행 검증)
================================================================
peerDependencies 실측:
  @module-federation/vite 1.20.7 → vite "^5 || ^6 || ^7 || **^8**"
  → 우리 web-spa 는 vite 8.1.5. **범위 안**

[실제로 지어서 돌렸다 — scratchpad 에 remote/host 두 앱]
  remote: exposes { './Badge': './src/Badge.js' }  → vite build **exit 0**
          dist/remoteEntry.js 생성(140 B)
  host  : remotes { remote_app: http://localhost:5301/remoteEntry.js }
          → vite build **exit 0**

  두 앱을 preview 로 띄우고(5301/5302, EADDRINUSE 0건)
  헤드리스 크롬 --dump-dom 으로 확인:
    → 화면에 **"[remote] hello from host"**
    = 따로 빌드된 앱의 코드가 실행 중에 HTTP 로 실려 와 그려졌다

  ⚠️ 헤드리스로 잰 이유: 확장 탭은 visibilityState hidden 이라 못 믿는다
     (H-3 캐논). 크롬 앱 번들은 이 맥에서 "Google Chrome 2.app"

================================================================
5절. ★ 독립 배포 — 통제된 변수로 증명
================================================================
호스트 번들 해시 (변경 전): index-AjwMjMBf.js
  → remote/src/Badge.js 만 고침 (호스트는 한 글자도 안 건드림)
  → **remote 만** vite build
  → 호스트는 재빌드도 재시작도 **안 함**
호스트 번들 해시 (변경 후): index-AjwMjMBf.js  ← **동일**

헤드리스 재확인: 화면에 "[remote v2 — 새로 배포됨] hello from host"

⇒ 호스트 산출물이 바이트 단위로 그대로인데 화면 내용이 바뀌었다.
   이것이 MF 가 파는 것의 전부다. 「같이 배포하지 않아도 된다」

================================================================
6절. ★★ 복잡도 세금 — 고정 비용이라 「나누는 값」이 결론을 정한다
================================================================
같은 기능(문자열 하나 반환)을 페더레이션 있음/없음으로 각각 빌드:

  [작은 앱]
    federated host : 107,956 B
    plain app      :     805 B
    세금 107,151 B — **134.1배**

  [양쪽에 동일한 코드 ~45 kB 를 더한 뒤]
    federated      : 131,313 B
    plain          :  46,263 B
    세금  85,050 B — **2.84배**

⚠️⚠️ 여기서 H-6 의 실수를 반복하지 않는다(「32 kB 를 절감치로 읽으면 스무 배
   틀린다」). 134배는 참이지만 **비율은 앱이 조금만 자라도 붕괴한다.**
   움직이지 않는 것은 세금의 **절대값(약 85~107 kB)** 이다.

⇒ 정확한 서술: "페더레이션은 앱 크기와 거의 무관한 **고정 비용**을 매긴다.
   그래서 작은 앱일수록 손해다 — 의견이 아니라 나눗셈이다."
   커리큘럼의 "소규모는 단일 앱이 더 빠름" 이 숫자로 선다.

빌드 시간도 같은 방향: plain 15ms / federated remote 243~637ms

================================================================
7절. 🚨 빌드는 초록인데 조용히 실패하는 것 (또 나왔다)
================================================================
remote·host 양쪽 빌드 로그에 매번:
  "Unable to compile federated types, Error: Command failed: ... tsc --showConfig"
  "error TS5058: The specified path does not exist: .../tsconfig.json"
  → 원격 모듈의 **타입 공유(dts)가 실패**한다
  → 그런데 **vite build 는 exit 0** 이고 remoteEntry.js 도 정상 생성된다

G-5(빌드 exit 0 인데 JS 청크 0개) · H-7(빌드 exit 0 인데 앱 전멸)
· H-8(MISSING_MESSAGE 인데 build 조용) 과 같은 계열.
이번 것은 **경계를 넘는 계약(타입)이 조용히 사라지는** 자리라
"원격은 남의 팀 것" 이라는 이 회차 주제와 정확히 맞물린다.

================================================================
8절. 🚨 우리 앱(web-next)은 MF 를 쓸 수 없다 — 실행으로 확인
================================================================
peer 실측: @module-federation/nextjs-mf@8.8.73
  next "^12 || ^13 || ^14 || ^15"   ← 우리는 **16.3.1**
  webpack "^5.40.0"                  ← Next 16 은 Turbopack 기본

실제 설치 시도 결과:
  npm error code ERESOLVE
  npm error Found: next@16.3.1
  npm error Could not resolve dependency:
  npm error peerOptional next@"^12 || ^13 || ^14 || ^15" from @module-federation/nextjs-mf@8.8.73

⇒ 우리 풀스택 앱은 **도구가 아직 안 따라와서** 이 아키텍처를 못 쓴다.
   "쓸지 말지" 를 고민하기 전에 "쓸 수 있는지" 가 먼저 걸리는 실사례.
   H-1 이 web-spa 를 무대로 고른 이유(Next 16+Turbopack 이슈)와 같은 계열이고,
   이 회차에선 그것 자체가 학습 내용이 된다.

================================================================
9절. 무대 결정 — scratch 에 짓고 되돌린다 (사용자 승인 2026-08-22)
================================================================
web-spa(vite 8)에서는 MF 가 실제로 돈다 → 진짜 코드로 가르칠 수 있다.
web-next 는 원천 차단(8절).

결정: `apps/web-spa/scratch/` 에 remote/host/plain 을 실제로 지어 전부 실측하고,
앱 본체·workspace·기준선(152/1061)은 건드리지 않는다.
근거 = H-6 이 DmPage 를 쪼개보고 되돌린 선례 · D-10 이 Docker 를 지어보고
Vercel 엔 배포 안 한 선례. **교안 결론(「우리 앱엔 안 넣는다」)과 코드 상태가 일치**한다.

⚠️ 저장 형식은 기존 관례를 따른다 — scratch 의 실험 코드는 `.txt` 로 박제된 것이
79건이다(F-2·F-3·F-4·G-1 선례). 이유가 이번에 하나 더 확인됐다:
  - tsconfig `include: ['src','vite.config.ts','vitest.config.ts']` → scratch 는 tsc 밖(안전)
  - **그러나 eslint 는 scratch 를 본다.** browser globals 블록이 `**/*.{ts,tsx}` 에만
    걸려 있어 `.js` 파일 안의 `document` 는 `no-undef` 로 잡힌다.
    node globals 블록도 `scratch/**/*.mjs` 전용이다.
  → 실행·측정은 scratchpad(저장소 밖)에서 하고, 산출 소스는 `.txt` 로 박제한다.

================================================================
11절. ★★ 실제 무대는 「좋아요 버튼」 — React 컴포넌트를 원격으로
================================================================
문자열 반환 수준이 아니라 **인스타 도메인의 진짜 조각**으로 다시 지었다.
  remote(`like_app`) : exposes './LikeButton' — useState 를 쓰는 React 컴포넌트
  host(`feed_app`)   : lazy + Suspense 로 원격 컴포넌트를 그린다
  둘 다 shared: { react: singleton, react-dom: singleton }

빌드: remote exit 0(remoteEntry.js **17.13 kB** · gzip 5.74) / host exit 0
런타임(헤드리스 --dump-dom):
  → `<button aria-pressed="false">♡ 좋아요 1,240개</button>`
  = 따로 빌드된 앱이 소유한 React 컴포넌트가 호스트 화면에 그려졌다
  ★ 자릿점(1,240)이 H-8 의 `toLocaleString` 결과 그대로라 회차 연결이 자연스럽다

⚠️ remoteEntry.js 가 140 B → 17.13 kB 로 커졌다. 공유 협상표가 실려서다.

================================================================
12절. ★★★ 브라우저가 실제로 받는 파일 24개 (세금의 다른 화폐)
================================================================
CDP 로 실측(`netprobe.mjs` · 의존성 0):
  버튼 **하나**를 그리는 데 호스트가 받는 JS = **24개 파일**
  출처가 둘로 갈린다 — localhost:5302(호스트 14) + localhost:5301(원격 10)
  이름에 `loadShare__react__` · `loadRemote__like_app_` 같은 협상 shim 이 그대로 보인다
  대조: 페더레이션 없는 같은 앱 = **JS 1개**

⇒ 세금은 바이트로만 오지 않는다. **요청 수·출처 수**로도 온다.
   (H-3~H-4 가 잰 LCP·INP 와 곧바로 이어지는 자리)

================================================================
13절. 🚨🚨🚨 `shared` 는 최적화가 아니라 **없으면 앱이 죽는 것**
================================================================
⚠️ 첫 측정이 틀렸다 — A/B 에 **서로 다른 grep 패턴**을 써서 잰 뒤
   "shared 있음이 더 큰데 React 사본은 0개" 라는 앞뒤 안 맞는 표를 얻었다.
   같은 자(React 본체 고유 문자열)로 다시 재니 결과가 **뒤집혔다**.
   → 캐논 재현: 「총계는 맞고 이름이 틀릴 수 있다」(H-6) · 재는 방법이 답을 정한다(H-3)

[같은 자로 다시 잰 값]
  shared 있음 : 총 JS 108,749 B · React 본체 포함 파일 **2개** · 청크 11개
  shared 없음 : 총 JS  84,520 B · React 본체 포함 파일 **0개** · 청크  7개

[런타임 — 이것이 결정타]
  shared 없는 원격을 호스트가 불러오면:
    → 버튼이 안 그려지는 정도가 아니라 **`<h1>피드</h1>` 까지 통째로 안 나온다**
      (lazy 가 던지면서 트리 전체가 죽는다)
    → 그런데 **remote·host 빌드는 둘 다 exit 0**
  shared 를 되돌리면 즉시 정상 복귀(버튼 재확인)

⇒ 두 가지를 동시에 가르치는 자리:
   ① `shared` 는 「중복 제거」 옵션이 아니라 **원격이 React 를 얻는 유일한 통로**
   ② **작아진 것이 좋아진 것이 아니다** — 84,520 B 가 108,749 B 보다 작지만
      그 작은 쪽은 아예 안 돈다. 번들 크기만 보면 정확히 반대로 읽는다
      (H-5·H-6 번들 3부작이 「숫자를 가려 읽어라」로 끝난 것과 정확히 이어진다)
   ③ 「빌드 초록인데 화면이 죽는다」 — G-5·H-7·H-8 에 이은 네 번째

================================================================
15절. 🚨 6절의 세금 수치는 폐기 — 측정 조건이 교안과 다르다
================================================================
6절(134.1배 / 2.84배)은 **React 가 없는 문자열 반환 앱**으로 잰 값이다.
교안 무대는 11절의 React 컴포넌트라 그 숫자를 그대로 인용하면
「측정 조건이 다른 값을 옮긴 것」이 된다(E-7 의 422px↔406px 계열).
→ **교안은 아래 16·17절 값만 인용한다.** 6절은 「비율은 분모가 정한다」는
   논증 용도로만 남긴다(그 결론 자체는 유효).

⚠️ 저장소에 박제한 `.txt` 를 그대로 복원해 다시 빌드한 뒤 잰 값이다
   (교안이 인용하는 코드 = 잰 코드).

================================================================
16절. ★★ 배포되는 바이트 (dist 기준 · React 포함 현실 앱)
================================================================
  대조군(페더레이션 없음) : 190,664 B / 청크 **1개**
  호스트(페더레이션)      : 319,662 B / 청크 **20개**
  원격(페더레이션)        : 108,749 B / 청크 **11개**
  호스트+원격 합계        : 428,411 B  → 대조군의 **2.25배**

⚠️ 원격 108,749 B 안에는 React **fallback 사본**이 들어 있다(13절).
   호스트가 React 를 주면 이 사본은 안 쓰인다 — 그런데 CDN 에는 놓인다.

================================================================
17절. ★★★ 브라우저가 실제로 받는 바이트 (사용자가 치르는 값)
================================================================
CDP `Network.loadingFinished` 의 encodedDataLength 합(=실제 전송량):

  대조군       : JS **1개**  ·  **59,882 B**
  페더레이션   : JS **24개** · **139,426 B**
                 └ 호스트(5302) 102,170 B + 원격(5301) 37,256 B

  세금 = **+79,544 B · 2.33배 · 파일 1개 → 24개**

★ 원격에서 받는 37,256 B 에 **React 는 없다** — 호스트 것을 쓴다(12절 목록 재확인).
  즉 fallback 사본은 «배포되지만 전송되지는 않는다».
  ⇒ 배포 바이트(2.25배)와 전송 바이트(2.33배)를 갈라 적어야 한다.
    한쪽만 인용하면 H-6 의 「32 kB 를 절감치로 읽으면 스무 배 틀린다」 재발.

================================================================
18절. ★ 독립 배포 최종 재현 (박제한 파일 기준)
================================================================
  [1] 화면: `<button aria-pressed="false">♡ 좋아요 1,240개</button>`
  [2] 호스트 번들: index-Jyffi1Md.js
  [3] 원격 «만» 고치고 원격 «만» 재빌드 (호스트 재빌드·재시작 0회)
  [4] 화면: `<button aria-pressed="false">🤍 좋아요 1,240개</button>`
  [5] 호스트 번들: index-Jyffi1Md.js  ← **동일**

⇒ 호스트 산출물이 바이트 단위로 그대로인데 사용자가 보는 것이 바뀌었다.

================================================================
19절. ⚠️ 회차 간 마감 요소 — H-8 에서 끊겼다 (D-6 계열 재발)
================================================================
        Step수 / 「한 줄 정리」 / 🙋토글 / 과제 [태그]형
  H-5:    5    /      5       /   5    /   4
  H-6:    5    /      5       /   5    /   4
  H-7:    6    /      6       /   6    /   4
  H-8:    8    /    **0**     / **3**  / **0**  ← 끊김

H-8 은 과제 헤더도 `### 1. 제목` 번호형이라 **헤딩 규약 위반**
(규약: 과제는 `### [구현]/[탐구]` 태그형 · 번호 금지).
파일 하나만 보는 게이트는 이것을 원리적으로 못 본다(D-6 FAIL 3 과 같은 자리).

→ **H-9 는 H-8 을 베끼지 않고 H-6·H-7 패턴을 복원한다** (7/7·7·4).
→ H-8 은 retrofit 큐에 올린다(학생 배포본이라 별건으로 처리).

================================================================
14절. 종료 시 상태
================================================================
포트 3000/8090/4173/5173/9000/5301/5302/9222 전부 EMPTY
저장소 워킹트리: 이 채증 파일 외 변경 0 (탐침은 전부 scratchpad)
web-spa 기준선 152/1061 무변경 (앱 코드 미변경)

================================================================
10절. Step 구성안 — full-granularity 기준
================================================================
병합 전 raw 로 펼친 활동(15~25분 단위):
  1. 원격 앱이 자기 코드를 내보낸다 (exposes · remoteEntry.js)
  2. 호스트가 실행 중에 그것을 가져온다 (remotes · 진짜로 화면에)
  3. 원격만 다시 배포해도 호스트가 그대로다 (독립 배포 · 해시 대조)
  4. React 를 두 번 싣지 않는 일 (shared · 버전이 어긋나면)
  5. 그 대가를 잰다 (고정 세금 · 134배 vs 2.84배)
  6. 경계를 넘는 계약이 조용히 깨진다 (dts 실패 · 원격이 안 뜨면)
  7. 우리 앱엔 왜 안 넣나 (Next 16 차단 + 판단표)

  → **raw 7 활동. 8 상한 이하라 분할 불필요.**

병합한 곳을 밝힌다(압축 아님):
  - 「MF 3.0 은 없다 + Native ESM 동향」은 커리큘럼이 **"1문단"** 으로 명시한
    분량이라 독립 Step 이 아니다 → Step 7 안에 넣는다.
  - 오프닝 후크(왜 나누고 싶어지나)는 이 과목 구조상 Step 이 아니다.

오프닝 진입: H-5=A · H-6=D · H-7=C · H-8=B → **E(인사)** 또는 D.
  Category H 종료 + 과목 60모듈 완주 회차라 G-3(코어 종료) 선례대로 E 가 맞다.
