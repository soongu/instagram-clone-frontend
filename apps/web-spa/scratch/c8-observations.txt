C-8 (WebSocket 클라이언트 — STOMP 실시간) 채증
================================================
착수일 2026-08-18 · 브랜치 module-C8-stomp (module-C7-performance 64aece2 에서 분기)


[1] 시의성 — npm registry 로 확인 (WebSearch 아님, C-4 교훈)
------------------------------------------------------------
@stomp/stompjs      7.3.0   latest · 2026-05-07 공개 · 런타임 의존성 0개
                            (7.4.0-beta.2 가 있으나 beta — stable 은 7.3.0)
sockjs-client       1.6.1   2022-10-22 이후 멈춤 — 우리는 안 쓴다(아래 [4])
@tanstack/react-query 5.101.4  C-6 과 동일
react               19.2.8  C-7 과 동일 (React 20 없음)
zustand             5.0.15  C-4 와 동일

커리큘럼 C-8 행에는 STOMP 클라이언트 라이브러리 이름이 아예 없었다 — 신설 필요.


[2] 착수 전제 확인 — 붙을 브로커가 있는가
------------------------------------------------------------
❌ 없다. C-5 와 같은 상황이 반복됐다.
  - 현행 spring-boot/instagram-clone-v2 : WebSocket 관련 파일 0건
  - 레거시 instagram-clone-with-jpa202502 : WebSocketConfig·StompController·
    StompChannelInterceptor 가 실물로 있다. 단 (a) 레거시라 v2 가 아니고
    (b) 봉투가 다르다(C-5 가 이미 확인한 사유).
  - spring-boot-advanced 는 curriculum.md 만 있고 교안·코드 0건
    (C-1~C-4 가 WebSocket·STOMP·DM·보안으로 예정만 되어 있다).

→ C-5 선례대로 apps/api-stub 에 STOMP 브로커를 세웠다. 의존성 0개.

레거시 백엔드에서 그대로 가져온 규약 (WebSocketConfig.java 실물):
  registry.enableSimpleBroker("/topic", "/queue");
  registry.setApplicationDestinationPrefixes("/app");
  registry.addEndpoint("/ws")
  → 우리 스텁도 /ws · /topic · /queue · /app 을 똑같이 쓴다.
  → StompController 의 /app/ping → /topic/pong 도 그대로 재현(백엔드 과목과 대조 가능).


[3] ⚠️⚠️ 손으로 짠 핸드셰이크가 두 번 거절당했다 — 둘 다 클라이언트가 알려줬다
------------------------------------------------------------
(a) "Server did not respond with sent protocols."
    stompjs 는 Sec-WebSocket-Protocol: v12.stomp, v11.stomp, v10.stomp 를 내밀고
    서버가 그중 하나를 골라 돌려주기를 기다린다. 안 돌려주면 연결을 끊는다.
    → selectProtocol 로 v12.stomp 를 골라 응답 헤더에 실었다.

(b) "Incorrect hash received in Sec-WebSocket-Accept header."
    ★ 내가 쓴 고정 문자열(GUID)이 틀렸다. 눈으로는 안 잡혔다.
      틀린 것: 258EAFA5-E914-47DA-95CA-5AB0DC85B11F   ← C 가 앞으로 가고 F 가 붙음
      맞는 것: 258EAFA5-E914-47DA-95CA-C5AB0DC85B11
    확인 방법: RFC 예시 키 dGhlIHNhbXBsZSBub25jZQ== 를 넣으면
      s3pPLMBiTxaQ9kYGzzhZRbK+xOo= 가 나와야 한다(맞는 것으로 하면 나온다).
    ★ 한 글자 자리바꿈을 사람 눈으로 찾으려 하지 말 것 — 규격이 준 시험값으로 대조한다.


[4] SockJS 를 안 쓰는 이유 (교안에서 한 줄로 짚을 것)
------------------------------------------------------------
레거시 백엔드는 .withSockJS() 가 붙어 있다. 그러나
  - sockjs-client 는 2022-10 이후 새 릴리스가 없다
  - WebSocket 을 못 쓰는 브라우저가 이제 없다
  - spring-boot-advanced 커리큘럼 C-2 도 "SockJS 불필요 이유" 를 항목으로 둔다
→ 우리는 날 WebSocket 으로 붙는다. 다만 학생이 회사 코드에서 만날 수 있으니 이름은 짚는다.


[5] 실물 대조 — @stomp/stompjs 7.3.0 이 우리 브로커에 붙는다 (probe 4/4)
------------------------------------------------------------
스크립트: apps/web-spa/scratch/c8-stomp-probe.mjs
서버: DELAY_MS=0 node apps/api-stub/server.mjs

  1) CONNECTED   ok
  2) SUBSCRIBE   ok
  3) /app/ping → /topic/pong
       {"response":"PONG: 안녕하세요","timestamp":1787016806523}
  4) REST 좋아요 → 열려 있는 연결에 푸시
       {"type":"like","postId":1,"likeCount":1241,"actor":"jaehoon"}

★ 4) 가 이 모듈의 핵심 교보재다. 좋아요는 여전히 C-6 의 REST 뮤테이션으로 가는데,
   결과가 누른 사람에게만 돌아오지 않고 열려 있는 모든 연결로 나간다.
   두 탭을 띄우면 한쪽에서 누른 것이 다른 쪽에 뜬다 — 흉내가 아니라 진짜 두 클라이언트다.

실제 오간 프레임 (stompjs debug 원문 — 교안 Step 4 교보재):
  >>> CONNECT
  login:jaehoon
  accept-version:1.2,1.1,1.0
  heart-beat:10000,10000

  <<< CONNECTED
  user-name:jaehoon
  session:sess-1
  heart-beat:10000,10000
  version:1.2

  >>> SUBSCRIBE
  id:sub-0
  destination:/topic/posts

  >>> SEND
  destination:/app/ping

서버 로그:
  [stomp] jaehoon 님이 연결했습니다 (sess-1) — 전체 1
  [stomp] 구독 /topic/posts (sess-1)
  [stomp] 연결이 끊겼습니다 (sess-1) — 남은 연결 0

⚠️ stompjs debug 에 "connected to server undefined" 가 찍힌다 —
   CONNECTED 프레임에 server 헤더가 없어서다. Spring 도 안 보낸다. 정상.


[6] Step 1 — 폴링 실측 (진짜 스텁 서버 · node scratch/c8-polling-measure.mjs)
------------------------------------------------------------
⚠️⚠️ 첫 측정이 틀렸다 — 6회가 전부 3403ms 로 똑같이 나왔다.
   원인: 좋아요를 바꾼 *직후부터* 폴링을 시작했다. 그러면 늘 "간격 한 번" 이 나온다.
   진짜 폴링은 우리가 언제 바꾸든 자기 시계대로 돈다.
   → 폴링을 계속 돌려두고, 바꾸는 시점을 주기 안에서 흩어서 다시 쟀다.
   ★ 재는 방법이 답을 정해버린 사고. C-7 의 "효과가 없으면 원인을 재고 갈라서 확인" 계열.

간격 3000ms · 8회 (서버 응답 지연 400ms 포함)
  가장 빨리 안 것  730ms
  가장 늦게 안 것  2993ms
  평균            2130ms  (간격의 0.71배)
  개별: 2749 / 1862 / 730 / 2993 / 2959 / 2056 / 1103 / 2591

→ 교안에 쓸 문장: "간격을 3초로 두면 남이 누른 것을 아는 데 평균 2초쯤 걸린다.
  빠를 때는 0.7초, 늦을 때는 3초다." (측정 조건: 스텁 서버 지연 400ms 포함)

요청 수는 산수로 따로 말한다(측정 아님):
  3초 간격 = 시간당 1200번. 실제로 달라지는 답이 하루 몇 번이면 대부분 헛걸음.

Vitest 판(src/queries/c8-polling.test.tsx · 3/3):
  - 아무도 안 바꿔도 요청은 계속 나간다
  - ★ 남이 바꾼 것을 아는 데 간격만큼 늦는다
  - 간격을 줄이면 빨리 알지만 요청이 그만큼 늘어난다
  가드 확인: refetchInterval 을 끄면 3건 전부 FAIL ✅


[7] Step 3~8 코드 — 실측·사고
------------------------------------------------------------
Step 3 (날 WebSocket): 스텁에 /ws-raw(에코) 신설. 데모는 src/components/RawSocketDemo.tsx
  (RefVsStateDemo·KeyDemo 선례대로 scratch 아님 — 교안이 경로를 달아야 한다).
  보여줄 것: 서버가 먼저 말을 걸 수 있다 / 오는 것은 문자열 하나뿐이라
  좋아요 소식과 쪽지를 가려낼 방법이 없다 → STOMP 가 이미 정해둔 규칙.

Step 5 어디에 두나: main.tsx 안, AppProviders 자식으로.
  ⚠️ 처음에 AppProviders 안에 넣었다가 되돌렸다 — withApp 을 쓰는 기존 판들이
  전부 진짜 WebSocket 을 열려고 든다. C-5 의 "네트워크를 안 보는 판에
  네트워크를 얹지 않는다"(MSW 전역 실패) 와 똑같은 함정.

★★ [8] C-7 회수가 실측으로 잡혔다 — eslint 가 곧 컴파일러였다 (재현)
------------------------------------------------------------
client.onConnect = () => {...} 로 대입했더니 eslint react-hooks/immutability:
  "Modifying a variable defined outside a component or hook is not allowed"

C-7 Step 4 가 말한 그대로인지 컴파일러로 직접 확인했다
(node scratch/c7-compile-out.mjs --summary):
  대입 형태            → 건너뜀 (캐시 0칸, 파일 통째로 포기)
  client.configure({}) → 캐시 4칸

  ConnectionIndicator 캐시 5칸 · RawSocketDemo 캐시 14칸

→ 라이브러리가 configure 를 열어둔 이유가 여기서 만난다. 대입 대신 configure.
   ★ C-7 의 "규칙을 지키는 것과 빠른 것은 따로가 아니다" 가 C-8 코드에서 재현됐다.

[9] Step 8 백오프 실측 (src/realtime/c8-stomp-connect.test.tsx)
------------------------------------------------------------
붙어보려 한 시각 사이의 간격: 552 → 1002 → 2002 → 4003 ms
  (reconnectDelay 500 · reconnectTimeMode EXPONENTIAL · maxReconnectDelay 30초)
가드 확인: LINEAR 로 바꾸면 2건 FAIL ✅

⚠️ 시의성 — 지수 백오프는 손으로 짤 필요가 없다.
  @stomp/stompjs 7.x 에 reconnectTimeMode(LINEAR|EXPONENTIAL) · maxReconnectDelay 내장.
  기본값은 reconnectDelay 5000 · LINEAR · maxReconnectDelay 15분.
  커리큘럼의 "재연결(지수 백오프)" 를 손수 구현으로 읽으면 시대착오가 된다.

[10] blast radius — 3건, 전부 내가 만든 것
------------------------------------------------------------
①·② c6-throw-on-error · c6-delete-rollback: "Found multiple elements with role status"
   원인: ConnectionIndicator 에 role="status" 를 달았다. 토스트가 이미 쓰는 역할이다.
   고침: 표시에서 role 을 뺐다(단언 무변경). 늘 떠 있는 표시는 "방금 일어난 일" 이 아니다.
③ b3-use-like-toggle "첫 화면 HTML 이 글자 하나 안 다르다":
   머리말에 표시가 붙어 보이는 글자가 늘었다.
   고침: withoutConnectionIndicator 스트리퍼 추가 — C-1(주요 메뉴)·C-6(로그인) 선례 그대로.
   스트리퍼가 실제로 걸렸는지 확인하는 단언도 함께(안 걸리면 비교가 아무것도 증명 못 함).

[11] 게이트 수치 (단독 실행)
------------------------------------------------------------
npm test   125 files / 901 passed   (C-7: 121 / 877)
tsc        0
eslint     0   (단 configure 로 고치기 전에는 immutability 1건 — 위 [8])
build      성공

번들:
  JS  index  499,920 → 524,641 B  (+24,721 · @stomp/stompjs)
      SignUpPage 100,007 B 그대로 (C-7 이 쪼개둔 것)
  CSS        42,488 → 43,421 B    (+933 · 연결 표시의 점 색깔 세 개 + size-2·gap-1.5)
      ★ C-3~C-7 은 내내 CSS 가 안 움직였는데(로직은 CSS 를 안 만든다) 이번엔 움직였다.
        머리말에 눈에 보이는 것을 하나 더 그렸기 때문 — C-6 의 +8 B(로그인 버튼) 와 같은 이유.

⚠️⚠️ C-7 이 없앤 500 kB 경고가 되돌아왔다.
  C-7 은 회원가입을 lazy 로 떼어내 499.92 kB 로 내려 경고를 없앴다.
  실시간 라이브러리가 +24.72 kB 를 얹어 524.64 kB 가 되면서 경고가 다시 뜬다.
  → 교안에서 정직하게 짚는다. 얻은 것(폴링 요청이 사라짐)과 낸 값(첫 화면에 25 kB)을
    나란히 놓는 자리다. C-7 의 "재보기 전에는 어느 쪽이 큰지 모른다" 와 이어진다.

[12] ★★ 브라우저 두 탭 실물 확인 (교안의 중심 주장)
------------------------------------------------------------
node apps/api-stub/server.mjs + vite(:5173) · 탭 두 개로 같은 주소를 연다.
서버 로그가 두 연결을 다 보여준다:
  [stomp] guest 님이 연결했습니다 (sess-1) — 전체 1
  [stomp] 구독 /topic/posts (sess-1)
  [stomp] guest 님이 연결했습니다 (sess-2) — 전체 2
  [stomp] 구독 /topic/posts (sess-2)

탭 A 에서 로그인하고 1번 게시물 하트를 누름 → 탭 A: 하트 빨강 · 좋아요 1241개
탭 B (아무것도 안 건드림 · 로그인도 안 함):
  ✅ 좋아요 1240개 → 1241개 로 바뀐다
  ✅ ★ 하트는 여전히 비어 있다 — 남이 누른 것이지 내가 누른 게 아니다
     (단위 테스트 "남이 누른 좋아요는 내 하트를 빨갛게 만들지 않는다" 가 브라우저에서 재현됨)
  ✅ 탭 제목의 "좋아요 N" 도 그대로 1 — 내가 누른 개수는 안 변했다

끊김·재연결도 실물로 확인:
  서버를 kill → 머리말 표시가 빨강 "연결 끊김" 으로 바뀐다
  서버를 다시 띄움 → 아무것도 안 눌렀는데 초록 "실시간" 으로 돌아오고,
  서버 로그에 두 탭의 구독이 다시 찍힌다(구독 복원).

⚠️ 시연 순서 주의 — 좋아요는 로그인해야 된다(C-6 의 SignInButton).
   로그인 없이 누르면 401 로 아무 일도 안 일어난다. 교안에 순서를 적을 것.
