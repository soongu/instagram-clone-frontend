E-5 Shadcn/UI 관찰 — shadcn CLI 4.17.0 · Tailwind 4.3.3 · Vite 8.1.5 · Chrome
================================================================================

E-4 채증(e4-dark-observations.txt)의 이어짐. 단위 테스트로 못 잡는 것만 적는다.

재현 (apps/web-spa 에서):
  rm -rf dist && npm run build
  LATEST=$(ls -t dist/assets/*.css | head -1)

시작 시점(E-4 종료 상태): CSS 11,467 B · :root,:host 29개 · globals.css 180줄
                        · 60 files / 452 passed


0. ⚠️⚠️ 시의성 — 커리큘럼 E-5 행의 전제 세 개가 전부 낡았다 (착수일 2026-08-12)
--------------------------------------------------------------------------------

  커리큘럼(2026-06 집필)          오늘 실측
  ------------------------------  ---------------------------------------------
  CLI 4.11.0                      4.17.0
  new-york 스타일 (default 폐기)  **스타일 개념 자체가 사라졌다** → 프리셋 8종
                                  nova/vega/maia/lyra/mira/luma/sera/rhea
                                  components.json 의 "style" 값은 이제
                                  "base-nova" · "radix-nova" 꼴 (베이스-프리셋)
  통합 radix-ui 로 접근성          2026-07 부터 **Base UI 가 기본값**
                                  -b base|radix|aria 3택. Radix 는 폐기 아님
                                  (모든 컴포넌트가 양쪽으로 계속 출시)

  근거: ui.shadcn.com/docs/changelog/2026-07-base-ui-default
        ui.shadcn.com/docs/changelog/2026-02-radix-ui
        npm view shadcn version → 4.17.0 / radix-ui → 1.6.7

  ⚠️ CLI --help 에 --style 플래그가 아예 없다. -b(베이스) 와 -p(프리셋) 뿐이다.
     "new-york 을 고른다" 는 서술은 오늘 시점에 재현이 안 된다.

  사용자 결정(2026-08-12): **Base UI 로 간다** — 학생이 오늘 init 을 치면 받는 것과
  일치시킨다. Radix 는 "같은 자리에 끼울 수 있는 다른 부품" 으로 한 자리에서 대조.


1. 두 베이스의 산출 차이 — CSS 는 바이트 단위로 같고, 컴포넌트 안쪽만 다르다
------------------------------------------------------------------------------

  빈 Vite 프로젝트에 각각 init 해서 diff 했다.

    diff radix-nova/src/index.css base-nova/src/index.css   → **차이 0줄**

  button.tsx 만 갈린다:

    radix-nova                          base-nova
    ------------------------------      ----------------------------------------
    import * as React from "react"      import { Button as ButtonPrimitive }
    import { Slot } from "radix-ui"       from "@base-ui/react/button"
    React.ComponentProps<"button">      ButtonPrimitive.Props
      & VariantProps & {asChild?}         & VariantProps
    const Comp = asChild ? Slot.Root     (asChild 없음)
      : "button"
    data-slot + data-variant + data-size  data-slot 만

  => 토큰·다크 이야기(E-4 와 잇는 자리)는 베이스 선택과 **무관**하다.
     의존성만 radix-ui ↔ @base-ui/react 로 갈린다.


2. init 은 경로 별칭이 없으면 아예 멈춘다 (선행 조건)
-------------------------------------------------------

  우리 프로젝트엔 @/ 별칭이 없었다. 그대로 돌리면:

    ✔ Verifying framework. Found Vite.
    ✔ Validating Tailwind CSS. Found v4.
    ✖ Validating import alias.
    Could not find valid path aliases or package imports for init.

  프레임워크와 Tailwind 는 알아서 찾아내지만 별칭은 우리가 깔아야 한다.

  ⚠️⚠️ baseUrl 은 쓰면 안 된다 — TypeScript 6.0 에서 deprecated 다:

    tsconfig.json(7,5): error TS5101: Option 'baseUrl' is deprecated and will
    stop functioning in TypeScript 7.0.

    많은 별칭 안내 글이 baseUrl + paths 를 함께 쓰라고 한다. paths 만으로 된다
    (tsconfig 위치 기준으로 풀린다). 확인: baseUrl 빼고 tsc --noEmit → 0.

  vite 쪽도 따로 필요하다(vite 는 tsconfig paths 를 안 읽는다).
  vitest.config.ts 가 vite.config 를 mergeConfig 하므로 테스트까지 한 번에 걸린다.


3. ⚠️⚠️ init 이 우리 토큰 세 개를 조용히 덮었다 — 테스트는 452개 전부 초록인 채로
--------------------------------------------------------------------------------

  init 은 globals.css 를 지우지 않는다. 앞에 import 4줄 + @custom-variant 를 얹고,
  우리 내용 **뒤에** 자기 블록을 통째로 덧붙인다. 180줄 → 308줄.

  뒤에 오니까 같은 이름은 shadcn 것이 이긴다.

  (1) --color-muted  ← 가장 아프다

    shadcn 의 @theme inline 에 --color-muted: var(--muted) 가 있다.
    @theme inline 은 변수를 안 내보내고 값을 유틸리티에 바로 박는다. 그래서
    빌드 CSS 에서 우리 --color-muted 선언이 **사라진다**:

      .text-muted{color:var(--muted)}        <- shadcn 의 --muted 를 가리킨다
      :root,:host 안 --color-muted           <- **없음**

    우리 9개 중 muted 하나만 겹쳤다. 나머지 8개(brand·danger·line·line-soft·
    subtle·ink·canvas·surface)는 그대로 살아 있었다. 전수 확인함.

    화면에서 잰 값 (text-muted 를 쓰는 요소 7곳):
      우리 값     oklch(64.7%) = rgb(142,142,142)   surface 위 대비 3.28
      덮인 값     oklch(0.97)  = rgb(245,245,245)   surface 위 대비 **1.09**
                                                    canvas 위 대비 **1.04**
      => 댓글 보조문구·섹션 제목이 사실상 안 보인다.

    ⚠️ 대비 3.28 은 E-4 채증 5번의 muted 라이트 값과 **정확히 일치**한다(교차 확인됨).
    ⚠️ canvas 위 1.04 는 E-4 의 ProfileImagePicker 사고와 **우연히 같은 숫자**다.
       원인이 다르니 섞어 쓰지 말 것. 본문은 surface 기준 3.28 → 1.09 로 말한다.

  (2) --font-sans

      우리 것   system-ui, -apple-system, 'Apple SD Gothic Neo', sans-serif
      덮인 것   'Geist Variable', sans-serif

    Geist 에는 한글 글리프가 없다. 화면이 깨지진 않고 sans-serif 로 떨어진다.
    => "한글이 깨진다" 고 쓰면 **거짓**. 정확히는 우리가 고른 글꼴이 아니게 된다.
    프리셋이 woff2 5개(약 76 KB)를 함께 들고 온다.

  (3) --radius-* ← 아무도 안 건드렸는데 모서리가 다 바뀐다

    shadcn 이 --radius: 0.625rem 을 두고 스케일 전체를 그 배수로 재정의한다.

      클래스        Tailwind 4.3.3 기본   shadcn 이후   우리 화면에서 잰 값
      rounded-sm    0.25rem  (4px)        0.375rem(6)
      rounded-md    0.375rem (6px)        0.5rem  (8)   입력칸 6 → **8px**
      rounded-lg    0.5rem   (8px)        0.625rem(10)  카드   8 → **10px**
      rounded-xl    0.75rem  (12px)       0.875rem(14)
      rounded-3xl   1.5rem   (24px)       1.375rem(22)  <- 오히려 작아진다
      rounded-4xl   2rem     (32px)       1.625rem(26)  <- 오히려 작아진다

    우리 앱의 rounded-* 사용: lg 7 · md 5 · full 2 (full 은 영향 없음)

  ⚠️⚠️ 이 셋 전부 **테스트가 못 잡았다**. init 직후 60 files / 452 passed 그대로.
     우리 테스트는 className 문자열을 보지 색을 안 본다. 초록인데 화면이 깨져 있었다.


4. 화해 — 겹친 이름만 고른다
--------------------------------

  (1) muted → **faint** 로 우리 것을 옮겼다.
      shadcn 의 muted 는 "흐린 배경면" 이고 우리 muted 는 "보조 글자색" 이라
      뜻이 다른데 이름만 같았다. shadcn 컴포넌트가 bg-muted 를 하드코딩해서
      오므로 양보하는 쪽은 우리다.
      덤: ink > subtle > faint 로 진하기 순서가 이름에 드러난다
      (E-4 가 남긴 "muted(#8e8e8e)가 subtle(#737373)보다 연한데 이름은 반대로
      읽힌다" 는 어색함이 여기서 풀렸다).

      손댄 곳: text-faint 7곳(App·PostBody×2·Section·KeyDemo×2·CommentList)
              + 테스트 3곳 + scratch/e2-b3-class-bridge.ts 6곳

      ⚠️ 브리지를 안 고치면 b3-use-like-toggle 과 b3-snapshots 두 개가 깨진다.
         b3BridgeHits() 가 "되돌리기가 실제로 걸렸는지" 를 확인하게 만들어 둔 덕에
         조용히 통과하지 않고 큰 소리로 실패했다. E-4 때와 같은 자리.

  (2) --font-sans 는 싸우지 않고 **합쳤다**:
        'Geist Variable', system-ui, -apple-system, 'Apple SD Gothic Neo', sans-serif
      로마자는 Geist, 한글은 뒤로 흘러 우리 글꼴. 우리 @theme 의 옛 줄은
      뒤 선언에 덮여 죽은 줄이라 지웠다.

  (3) --radius-* 는 **받아들였다**. 들여온 컴포넌트와 모서리를 맞추는 게 낫다.
      단 "모르고 당한 것" 과 "알고 받아들인 것" 은 다르다 — 위 표로 재고 넘어간다.

  화해 후 재측정: text-faint rgb(142,142,142) · canvas 위 3.14 (E-4 값 복귀)
                 글꼴 스택에 Apple SD Gothic Neo 돌아옴
                 shadcn 의 --muted(0.97)는 자기 컴포넌트용으로 그대로 살아 있다


5. ⚠️⚠️ @custom-variant — E-4 가 예고한 것과 shadcn 이 쓴 것이 다르다
-----------------------------------------------------------------------

  E4-lecture.md:855  @custom-variant dark (&:where(.dark, .dark *));   <- 문서 형태
  shadcn 이 써넣은 것 @custom-variant dark (&:is(.dark *));            <- 실물

  둘의 차이를 브라우저에서 직접 갈라 봤다:

    (가) .dark 를 단 요소 **자신**
         :is(.dark *)             안 걸림   (자손만 본다)
         :where(.dark, .dark *)   걸림      (자기 자신도 목록에 있다)

    (나) 자손 — 둘 다 걸림

    (다) 특정도 — 뒤에 오는 같은 급 클래스 규칙과 겨루면
         :is    이김   ( :is 는 인자 중 가장 센 것의 특정도를 가져온다 → (0,2,0) )
         :where 짐     ( :where 는 항상 0 → (0,1,0) 동률 → 나중 것이 이김 )

  => shadcn 이 :is 를 쓰는 이유가 (다)다. dark: 유틸리티가 평소 유틸리티에
     특정도로 이기게 하려는 것.
  ⚠️ E-4 는 문서 형태를 옮겨 적었고 그건 그때 실행 검증까지 했다(E-4 채증 13번).
     둘 다 동작하지만 같지 않다. 교안에서 "지난 시간에 본 그 줄" 이라고 뭉뚱그리면
     학생이 자기 파일에서 다른 글자를 보고 혼란스러워한다.

  ⚠️ 우리 앱은 dark: 유틸리티를 0개 쓴다(e4 테스트가 not.toMatch(/dark:/) 로 강제).
     그래서 이 변형 줄은 **지금은 아무 일도 안 한다**. 실제로 일하는 것은 6번의
     .dark 규칙이다. 변형이 일하기 시작하는 것은 shadcn 컴포넌트가 들어오는 다음 강의.


6. 조건을 옮겼다 — 값은 한 개도 안 바꿈
-----------------------------------------

  before  @media (prefers-color-scheme: dark) { :root { --color-*: ... } }
  after   .dark { --color-*: ... }

  빌드 CSS 확인:
    .dark{--color-canvas:oklch(0% 0 none);--color-surface:oklch(18.2% 0 none);
          --color-ink:oklch(97% 0 none);--color-faint:oklch(73.2% 0 none);
          --color-subtle:oklch(83% 0 none);--color-line:oklch(33.3% 0 none);
          --color-line-soft:oklch(26.9% 0 none)}
    "prefers-color-scheme" 등장 횟수 **0**
    CSS 17,855 → 17,819 B (−36, @media 감싸개가 빠진 만큼)

  우리 .dark 블록 바로 뒤에 shadcn 의 .dark 블록이 따로 온다. 겹치는 속성이 없어
  둘 다 산다(우리는 --color-*, 저쪽은 --background 계열).

  손으로 붙였다 뗀 확인 (className 은 한 글자도 안 고쳤다):
    표시 없음 → body 0.985 / 카드 1.0     / 테두리 0.891 / faint 0.647
    .dark 붙임 → body 0     / 카드 0.182  / 테두리 0.333 / faint 0.732
    다시 뗌   → 처음 값과 완전히 동일
  값은 E-4 팔레트 그대로다.


7. 세 갈래 고르개 — '시스템' 은 "안 고르겠다" 로 돌아올 길
-----------------------------------------------------------

  고른 것(choice: light|dark|system)과 실제로 그릴 것(resolved: light|dark)을
  나눠 뒀다. 시스템을 고른 사람만 matchMedia 알림을 따라간다.

  브라우저 실측 (OS 는 라이트):
    처음        표시 (없음)  눌린 것 시스템   저장값 null
    어둡게 클릭  표시 dark    눌린 것 어둡게   저장값 "dark"
    밝게 클릭    표시 (없음)  눌린 것 밝게     저장값 "light"
    시스템 클릭  표시 (없음)  눌린 것 시스템   저장값 "system"

  고른 칸 색은 bg-ink text-canvas 로 뒀다 — E-4 가 토스트에서 쓴 그 짝이라
  다크에서 저절로 뒤집힌다(다크에서 잰 값: 배경 oklch(0.97) 글자 oklch(0)).

  ⚠️ jsdom 에는 matchMedia 가 **아예 없다**(typeof → undefined). App 을 렌더하는
     테스트가 15개쯤 되므로 test-setup.ts 에 "시스템은 밝게" 로 답하는 최소 stub 을
     깔았다. 시스템이 바뀌는 상황은 그 테스트가 vi.stubGlobal 로 자기 것을 끼운다.
  ⚠️ matchMedia 리스너가 부르는 setState 는 act() 밖이라 안 흘러간다.
     await act(async () => system.change(true)) 로 감싸야 통과한다.
     (production 버그가 아니라 테스트 하네스 문제 — 브라우저에서는 잘 돈다.)
  ⚠️ App 머리말에 고르개가 붙어 b3-use-like-toggle 의 "글자 하나 안 다르다" 비교가
     깨진다. B-5 가 withoutSignUpSection 을 만든 선례대로 withoutThemeToggle 추가.


8. 깜빡임 — 재고 나서 고쳤다
-------------------------------

  index.html 에 임시 채증 스크립트를 넣어 "표시가 언제 붙는지" 를 직접 봤다.
  저장값 dark 로 새로고침:

    회차   파싱 시점 표시   DCL 시점 표시   DCL(ms)   React가 붙인 시각(ms)   틈
      1      (없음)          (없음)          30.1            80.5           50.4
      2      (없음)          (없음)          39.0            42.6            3.6
      3      (없음)          (없음)          34.1            55.9           21.8

  ⚠️ 틈의 길이는 3.6 ~ 50 ms 로 회차마다 널뛴다. **숫자를 하나로 못 박지 말 것.**
     변하지 않는 것은 왼쪽 두 칸이다 — 파싱·DCL 시점에 표시가 **한 번도 없었다**.
     로컬 빌드라 이 정도지 느린 기기·느린 망에서는 더 벌어진다.

  고침: head 에 인라인 스크립트(모듈 아님·defer 아님). 고친 뒤 재측정:
    파싱 시점 표시 dark · DCL 시점 표시 dark · React 는 61.3ms 에 같은 값을 또 쓴다(무해)

  ⚠️ 이 몇 줄은 모듈을 못 부른다(부르는 순간 기다리게 되어 넣은 뜻이 없어진다).
     그래서 'ig-theme' 이라는 이름과 판단 규칙이 lib/theme.ts 와 **두 곳에 산다.**
     이 중복이 갈라지는 것을 e5-first-paint.test.ts 가 막는다.

  ⚠️⚠️ 그 테스트를 처음엔 toContain(THEME_STORAGE_KEY) 로 썼는데 **쓸모가 없었다.**
     이름을 'ig-theme-OOPS' 로 바꿔놓고 돌려보니 5개 전부 통과했다 —
     'ig-theme-OOPS' 가 'ig-theme' 을 부분 문자열로 품기 때문.
     따옴표까지 함께 보게( `'${THEME_STORAGE_KEY}'` ) 고친 뒤에야 잡혔다.
     => 교훈: **가드를 만들었으면 일부러 깨서 진짜 잡히는지 확인할 것.**
        통과하는 테스트는 아무것도 증명하지 않는다.


9. 새로 깔린 의존성 — "npm 의존이 아니다" 를 정직하게 말하려면
-----------------------------------------------------------------

  init 이 package.json 에 넣은 것 8개:
    @base-ui/react 1.7.0          동작·접근성 알맹이
    class-variance-authority 0.7.1 variant 를 클래스 묶음으로
    clsx 2.1.1 + tailwind-merge 3.6.0  cn() 의 두 재료
    lucide-react 1.31.0           아이콘
    tw-animate-css 1.4.0          애니메이션 유틸
    @fontsource-variable/geist 5.3.0  프리셋 글꼴
    shadcn 4.17.0                 (devDependency 아니라 dependency 로 들어온다)
  + 우리가 별칭 때문에 넣은 @types/node 24

  ⚠️ "복사해 소유한다 = npm 의존이 없다" 가 아니다. **컴포넌트 소스**가 내 repo 로
     오는 것이고, 그것이 기대는 부품은 여전히 npm 에서 온다.
     이걸 얼버무리면 학생이 package.json 을 열었을 때 배신감을 느낀다.


10. 최종 게이트 (모듈 종료 시점)
----------------------------------

  npm test          63 files / 475 passed   (E-4: 60 / 452)
  tsc --noEmit      0
  eslint .          0
  vite build        성공
  CSS               11,467 → **18,038 B**   (+6,571)
  JS                → 306,516 B
  globals.css       180 → 306줄
  커밋              5개 (Step 2 / 3 / 4 / 5 / 6)
  Step 1 은 코드 변경 없음(철학·대안 비교)

  ⚠️ CSS 증가분 대부분은 shadcn 의 토큰 블록과 tw-animate-css 다. 아직 화면에
     쓰는 shadcn 컴포넌트는 **0개** 인데도 이만큼 는다. 다음 강의에서 컴포넌트를
     넣으면 또 는다 — 그때 다시 재서 "무엇이 얼마를 차지하는지" 를 보여줄 것.

  blast radius: scratch/e2-b3-class-bridge.ts 6항목(text-muted→text-faint)
                + b3-use-like-toggle.test.tsx (withoutThemeToggle 추가)
                + test-setup.ts (matchMedia stub · 테스트 간 표시/저장소 청소)

  ⚠️ 다음 강의 주의: e4-dark-mode.test.tsx 가 App 에 dark: 가 0개임을 강제한다
     (expect(html).not.toMatch(/dark:/)). shadcn 컴포넌트는 내부에 dark: 를
     쓰므로 그것들을 App 에 넣는 순간 이 단언이 깨진다. 그때 단언 범위를
     "우리가 쓴 컴포넌트" 로 좁혀야 한다. 미리 알고 있을 것.
